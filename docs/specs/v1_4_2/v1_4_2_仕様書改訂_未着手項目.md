# v1.4.2 仕様書改訂 未着手項目

**作成日**：2026-05-13
**作成担当**：仕様書改訂担当オーパス君（5/13 引き継ぎ後）
**位置づけ**：2026-05-13 のストック反映作業（65 件）でスコープ外とした項目、および将来の別タスクとして残した項目の引き継ぎメモ。次担当者・たんたん自身が起点として参照する。

---

## 1. Contact 詳細画面 AJAX 化に伴う新仕様の追加反映

**対象**：統合最終版（`名刺画像取り込みOCR仕様書_v1_4_2統合最終版.md`）

### 経緯

ストック #20「Contact 詳細画面 AJAX 化に伴う設計方針大転換」のうち、**廃止 3 項目**（Execute_Merge_with_Updates / マージ画面値編集 UI / MergeForm 値修正メソッド）は 2026-05-13 のストック反映で本体に反映完了した。

しかし、設計大転換の **新仕様部分** は D-3 系 AJAX 実装完了後の別ストック追記 → 仕様書反映の流れとして今回はスコープ外にした。

### 未着手の新仕様項目

| 項目 | 反映対象章節（想定） |
|---|---|
| `contact.update_field(field_name, new_value, user)` モデルメソッドの詳細仕様（§10.6.4 ケース 4 派生として記述、または §10.5.x として独立節） | §10.5 / §10.6.4 |
| Contact 詳細画面の AJAX 3 エンドポイント（`ajax-update-field` / `ajax-confirm-fields` / 3 つ目は実装側を要確認）の URL / 入出力 / 認証ガード仕様 | §11.3 URL 一覧（24 / 25 / 26 番として末尾追加候補）/ §11.4 ビュー層設計 |
| ContactDetailView の編集可能モード判定の確定仕様（View 層ガードと AJAX エンドポイント側の認証ガードの整合） | §11.4 / §10.5 |
| マージ画面の新構造詳細（中央判定情報入力ブロックの内訳）：§11.5.5 〜 §11.5.7 で書ききれなかった JS なしの値違い見比べ UI、確認チェック CB の動的表示、エラーサマリ表示の詳細 | §11.5.5 〜 §11.5.7 / §11.8.6 |

### 進め方

1. D-3 系 AJAX 実装の完了確認（A 側サポート担当に確認）
2. AJAX 実装で確定した URL / 入出力 / View 層ガードを A 側サポートからヒアリング
3. ストック追記 → 仕様書反映の通常フローで対応
4. 反映時には §11.3 URL 一覧の連番ルール（v1.4.2 では No.22 / 23 まで使用済み）に従い末尾追加

---

## 2. BackNavigator 使い方ガイドの Markdown 化

**対象**：`docs/BackNavigator使い方ガイド.docx`

### 経緯

ストック #26「BackNavigator 使い方ガイド §9 画面遷移イメージの修正（`push_current(title, [])` の実装非整合解消）」が未着手。

理由：現状の `BackNavigator使い方ガイド.docx` は Word 形式のため、Claude Code から直接編集できない。たんたんから「ドキュメントとしては Markdown 形式で残れば OK、PDF / Word 化は別途スクリプトで対応する」との方針確定（2026-05-13）。

### 未着手の作業

| 項目 | 内容 |
|---|---|
| .docx → .md 化 | `BackNavigator使い方ガイド.docx` の内容を `docs/BackNavigator使い方ガイド.md`（または同等のパス）として書き起こす |
| ストック #26 の修正反映 | §9 画面遷移イメージで `push_current('ブログ詳細', [])` と書かれている記述を、実装慣例（既存 cards 詳細画面は `push_current` を呼ばず `BackNavigator(request)` の生成のみ）に合わせて修正 |
| §8 NG パターンとの整合性確認 | 「keys を空にする」NG パターン記述と §9 修正後の例文が矛盾しないか確認 |

### 進め方

1. 現状の .docx の内容を読み取る（たんたんが手動 or 変換ツールで md 化）
2. ストック #26 の修正方針（クロード君推奨は案 A「詳細画面の例から push_current 呼び出しを削除」）を反映
3. .md ベースで Git 管理に切り替え

---

## 3. PDF / Word 化スクリプトの整備（仕様書改訂タスク外、将来検討）

**対象**：v1.4.2 仕様書群（統合最終版 .md / v0.1.5 詳細 .md / URL 一覧表 .md / BackNavigator ガイド .md）

### 経緯

`URL一覧表_v1_4_2.md`（2026-05-13 新規作成）が正本になったため、旧 `URL一覧表_v1.4.2.pdf` の更新が必要。たんたんの方針「PDF / Word 化はドキュメント上では不要、別途スクリプトを作って変換する」（2026-05-13）に従い、PDF 生成は仕様書改訂タスクとしては扱わない。

### 関連スクリプト

- `docs/specs/build_scripts/build_spec.py`：仕様書 .md → .docx 変換用（既存）
- PDF 変換は未整備、別途スクリプトで対応する想定（たんたん作業）

### 未着手の作業

| 項目 | 内容 |
|---|---|
| PDF 化スクリプトの整備（任意） | .md → .pdf の変換フローを整備（CSS / レイアウト / Word 書式準拠 / KeepTogether 等の CLAUDE.md §11 要件を考慮） |
| URL 一覧表 PDF の旧版の扱い | `URL一覧表_v1.4.2.pdf` を削除するか古い版として残すか、たんたん判断 |
| .docx 出力フローの再評価 | `build_spec.py` は v1.4.2 改訂内容（章節追加・別表新規）に対応しているか確認 |

---

## 参照

- 反映完了済みストック：旧 `仕様書改訂_変更点ストック_作業中.md`（2026-05-13 削除、git 履歴で参照可）
- 反映マッピング：旧 `反映マッピング_作業中.md`（2026-05-13 削除、git 履歴で参照可）
- 本タスクのコミット履歴：`claude/review-spec-updates-4WfnN` ブランチの 13 コミット + 整合性チェック修正
- 関連スクリプト：`docs/specs/build_scripts/build_spec.py`
