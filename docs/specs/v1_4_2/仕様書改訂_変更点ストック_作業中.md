# 仕様書改訂 変更点ストック（作業中）

**対象仕様書（複数）**：
- 名刺画像取り込みOCR仕様書_v1_4_2統合最終版.md（**統合最終版**と略記）
- Run_Generate_Duplicate_Candidates_詳細仕様書_v0_1_5.md（**v0.1.5 詳細**と略記）
- BackNavigator使い方ガイド.docx（**BackNavigator ガイド**と略記）

**作成日**：2026-05-07
**最終更新日**：2026-05-11
**作成担当**：仕様書改訂担当オーパス君（Claude Opus 4.7）
**ストック件数**：45件

---

## 本ファイルの目的

v1.4.2 関連の仕様書（統合最終版、v0.1.5 詳細仕様書、および BackNavigator 使い方ガイド）に対して、実装作業中に確定した修正点・変更点を蓄積する作業中の備忘録である。実装が完了した段階で、本ストックを基に正式な改訂提案メモを対象仕様書ごとに分けて作成する。

【本ファイルの位置づけ】

- **作業中の備忘録**であり、最終ドキュメントではない
- 新しいクロード君（context 枯渇後の引き継ぎ、他チャットの参照）が変更箇所を分かるようにするための共有資料
- 実装中に変更が出るたびに追記していく
- 最終的な改訂提案メモが完成した時点で、本ファイルは削除する

【更新ルール】

- 変更点が確定するたびに、たんたんから情報をもらって項目を追加する
- 各項目は対象仕様書・確定経緯・担当ブランチ・ステータスを明示する
- 確定していない事項は「要確認」マークで明示する

【マージ・反映の運用方針】

- 本ストックの項目（#1〜#44）はすべて v1.4.2 リリースの範囲として扱う
- v1.4.2 関連項目は A 側ブランチ（feature/v1.4.2-models）および B 側ブランチ（feature/v1.5.0-pipeline-split、ブランチ名は履歴的事実）が main にマージされた段階で順次仕様書反映する
- 反映順序：A 側 → B 側 のマージ順序を前提とし、衝突調整（マイグレーション番号等）は B 側マージ時に対応する
- 両ブランチが main にマージされて 1 つの v1.4.2 リリースとして出す

---

## ストック一覧（サマリー）

| # | 対象仕様書 | 項目 | 担当ブランチ | ステータス |
|---|---|---|---|---|
| 1 | 統合最終版 | has_minimum_info NG 時の BusinessCard 残置（方針確定・要確認事項解消） | B 側 | 方針確定、ステップ1実装中 |
| 2 | 統合最終版 | DuplicateCandidate の match_reason / matched_fields / MatchReason 削除 | A 側 | 実装済み |
| 3 | 統合最終版 | ActionLog の配置先：duplicates → actionlogs として独立 | A 側 | 実装中 |
| 4 | 統合最終版 | ActionLog の diff フィールド削除 | A 側 | 実装済み |
| 5 | 統合最終版 | ActionLog の extra → data 改名 | A 側 | 実装済み |
| 6 | 統合最終版 | 別表 C.1 の OriginalImage.STATUS_CHOICES 表示名を実装に合わせる | 共通 | 実装済み（仕様書修正のみ） |
| 7 | 統合最終版 | §8.3 スコア表の具体値記載／§8.4 ランク判定の判定順序とAND関係明示／所属5フィールドの内訳明示 | A 側 | 確定 |
| 8 | 統合最終版 | §8.7 代表メール判定のバリエーション記述拡張 | A 側 | 確定 |
| 9 | 統合最終版 | BusinessCard モデルに ocr_result フィールド新規追加（5値TextChoices） | B 側 | 実装済み（コミット 3ceacb6） |
| 10 | 統合最終版 | §15.1 cards 処理ルール一覧の改訂（C2／C3／C5） | B 側 | 実装済み（コミット 3ceacb6） |
| 11 | 統合最終版 | §4.3 / §4.4 で「Contact のない BusinessCard が存在しうる」仕様を明文化 | B 側 | 方針確定 |
| 12 | 統合最終版 | §4.2 detected_count の意味を「DBに保存されたBC数」と明文化 | B 側 | 方針確定 |
| 13 | 統合最終版 | DebugMask モデル新設（OpenCV検出パイプラインのデバッグ用マスク画像をDB管理） | B 側 | 実装済み（コミット 3ceacb6） |
| 14 | 統合最終版 | ocr_result_badge カスタムタグの新規追加 | B 側 | 実装済み（コミット 3ceacb6） |
| 15 | 統合最終版 | CardListView に ocr_result フィルタ機能追加 | B 側 | 実装済み（コミット 3ceacb6） |
| 16 | 統合最終版 | CardDetailView の sibling_cards を通常表示エリアに移動 | B 側 | 実装済み（コミット 3ceacb6） |
| 17 | 統合最終版 | CardDeleteView の新規追加（名刺ハード削除機能） | B 側 | 実装済み（コミット 3ceacb6） |
| 18 | 統合最終版 | OriginalDetailView セクション7「検出された名刺」の改修 | B 側 | 実装済み（コミット 3ceacb6） |
| 19 | v0.1.5 詳細 | X-3 ランナバグ修正に伴う v0.1.5 詳細仕様書の改訂 | A 側 | 実装済み（コミット 4cd1c3e） |
| 20 | 統合最終版 | Contact 詳細画面 AJAX 化に伴う設計方針大転換（廃止確定 3 項目を包含） | B 側 | 廃止確定、新規仕様は D-3 実装後に追加予定 |
| 21 | 統合最終版 | Person.mark_as_active() メソッドの新規追加（mark_as_merged との対称ペア命名確定） | A 側 | 実装済み（コミット cef83c4） |
| 22 | 統合最終版 | §12.8.3 / §9.3.1 / §9.5.2 のマージ復元処理フロー記述修正 | A 側 | 方針確定 |
| 23 | 統合最終版 | Person.transfer_contacts_to() の挙動明文化（Case A〜D ステータス遷移との対応） | A 側 | 実装済み（コミット 62a8ea9） |
| 24 | 統合最終版 | PersonDetailView の役割変更（active は ContactDetailView へリダイレクト、merged / archived は専用詳細画面） | A 側 | 方針確定 |
| 25 | 統合最終版 | ContactDetailView の業務メイン画面化と新規セクション追加（他のアクティブコンタクト / マージ関連） | A 側 | 実装済み（コミット fec32d4） |
| 26 | BackNavigator ガイド | BackNavigator 使い方ガイド §9 画面遷移イメージの修正（push_current(title, []) の実装非整合解消） | 共通 | 実装側完了（ガイド修正のみ） |
| 27 | 統合最終版 | Contact.full_name の空文字制約追加（DB 制約 + Form clean + AJAX View ガード） | A 側 | 方針確定、未実装 |
| 28 | 統合最終版 | CardDetailView の業務画面化（Contact 編集 UI 追加、_contact_field.html パーツ再利用） | A 側 | 実装済み（コミット c1c7686） |
| 29 | 統合最終版 | ContactListView 新規追加（§11.3 / §20.1 改訂、検索フォーム付き一覧画面） | A 側 | 実装済み（コミット 53ff061） |
| 30 | 統合最終版 | ContactDetailView に inactive Contact 履歴セクション追加（マージ関連セクション内、共通 include 化） | A 側 | 実装済み（コミット 61cfd91） |
| 31 | 統合最終版 | ContactDetailView に「現在所属 Person への遷移リンク」追加 | A 側 | 実装済み（コミット fdb3805） |
| 32 | 統合最終版 | v1.2.0 パターンA 撤回（OCR 単位を 1 OriginalImage → 1 BusinessCard に変更） | B 側 | 実装済み（feature/v1.5.0-pipeline-split） |
| 33 | 統合最終版 | BusinessCard モデル拡張（raw_json_1 / raw_json_2 / ocr_status / claimed_at / error_message 追加、ocr_result null 許容化） | B 側 | 実装済み（コミット 440ed4f） |
| 34 | 統合最終版 | OriginalImage.STATUS_CHOICES 拡張（opencv_processing / cards_extracted 追加） | B 側 | 実装済み（コミット 440ed4f） |
| 35 | 統合最終版 | OriginalImage.raw_json の読み出し廃止（フィールド物理残置） | B 側 | 実装済み（コミット 0dd8e5f / 2c35db0） |
| 36 | 統合最終版 | パイプライン関数構造の刷新（Run_Crop_Cards_From_OriginalImage / Run_Process_CardImages_With_OCR / process_cardimage_with_ocr / extract_carddata_via_ocr / _rotate_card_image） | B 側 | 実装済み（コミット 65759d8 / 241cf42 / f426cd2 / d25b47d） |
| 37 | 統合最終版 | 条件付き 2 回 OCR の導入（1 BC につき orientation に応じて 1〜2 回 API 呼び出し） | B 側 | 実装済み（コミット 241cf42） |
| 38 | 統合最終版 | process_opencv 管理コマンド新設（OpenCV パイプライン専用 cron） | B 側 | 実装済み（コミット 65759d8） |
| 39 | 統合最終版 | process_ocr 管理コマンド新設（OCR パイプライン専用 cron） | B 側 | 実装済み（コミット 7c41ebf / d25b47d） |
| 40 | 統合最終版 | retry_failed_ocr の --opencv / --ocr 分岐、ocr_recovery.py 共通ヘルパー新設 | B 側 | 実装済み（コミット c944be2） |
| 41 | 統合最終版 | card_image の同期書きへの変更（save_card_image_tmp / on_commit リネーム廃止） | B 側 | 実装済み（コミット 65759d8 / 3c76348） |
| 42 | 統合最終版 | 旧 process_pending / PipelineCoordinator の完全削除 | B 側 | 実装済み（コミット 3c76348） |
| 43 | 統合最終版 | UI 改修（7値フィルタ、ocr_status バッジ、andypf-json-viewer 採用、OriginalImage.raw_json 表示廃止） | B 側 | 実装済み（コミット 0dd8e5f / 7b467ac / 2c35db0） |
| 44 | 統合最終版 | データマイグレーション 0011（旧 OriginalImage.raw_json → BC.raw_json_1 移行） | B 側 | 実装済み（コミット 9ce638c） |
| 46 | 統合最終版 | ContactUpdateForm 動的確認 CB の追加条件「confirmed_at is None 限定」明文化（§11.6.2 / §11.7.1） | A 側 | 実装済み（コミット 151f432） |
| 47 | 統合最終版 | Contact.lang に blank=True 明文化（§4.x Contact モデル定義表） | A 側 | 実装済み（コミット 32ef99d） |
| 48 | 統合最終版 | §10.4.3 行 1117 削除（fix での set_primary_contact 記述、§11.4.1 との矛盾解消） | A 側 | 実装済み（コミット 1caec0a、§11.4.1 を正として実装） |
| 49 | 統合最終版 | 15 番 DuplicateCandidateGroupListView 絞り込み仕様 + サイドバー追加の明文化（§11.3 / §11.5 / §20） | A 側 | 実装済み（コミット 4f605c8） |

---

# 各項目の詳細

## #1 has_minimum_info NG 時の BusinessCard 残置（方針確定・要確認事項解消）

**対象仕様書**：統合最終版

### 項目名
has_minimum_info で弾かれたケースでも BusinessCard を生成する仕様への変更（方針確定）

### 現状仕様（仕様書の該当章節）

**第15章 §15.1 cards 処理ルール一覧**（行 2497-2507）

| ID | ケース | 処理 |
|---|---|---|
| C3 | 全 card が has_minimum_info で弾かれた | status=extracted（BusinessCard 0 件）、error_message に記録 |

### 改訂内容（確定版）

C3 の処理を以下に変更する。

- has_minimum_info で弾かれた card に対しても **BusinessCard は生成する**
- ただし **Contact / Person は作らない**
- BC の `ocr_result` フィールドに `'insufficient_info'` をセット（#9 参照）
- error_message には記録しない（silent skip、コードが現実、案A方針）

詳細な §15.1 改訂内容は #10 を参照。BusinessCard モデル変更は #9 を参照。

### 改訂理由

1. **ユーザー使い勝手**：撮り直し判断の材料として「OCR が何を読み取ったのか」を確認できる
2. **OpenCV データ収集**：検出精度改善のため、has_minimum_info NG ケースの実データを蓄積する
3. **デバッグ性**：処理した事実を BusinessCard として残すことで、後追い検証が可能になる

### 影響範囲

- §15.1 ルール C3 の処理内容修正（→#10）
- §4.3 BusinessCard モデルへの ocr_result フィールド追加（→#9）
- §4.3 BusinessCard と §4.4 Contact の関係性の整理（→#11）
- §14.4 モデル固有の TextChoices に BusinessCard.OcrResult を追加（→#9）
- 別表 C に BusinessCard.OcrResult セクション追加（→#9）
- 別表 A.4 BusinessCard フィールド対照表に ocr_result 追加（→#9）
- マイグレーション必要（BusinessCard テーブルに ocr_result カラム追加）

### 確定経緯

- 2026-05-07 たんたんから方針確定の連絡（B 側サポートチャットでの提案を受けて）
- 2026-05-07 たんたんと B 側サポートクロード君で方針詳細を詰め、要確認事項3点解消
- 2026-05-07 B 側サポートクロード君から仕様書改訂担当オーパス君へ報告書受領

### 担当ブランチ

**B 側（feature/opencv-improvement）** ※初版で「A側または要確認」としていたが、B 側に確定

理由：A 側は Contact / Person / Duplicate / ActionLog 関連の作業、B 側は OCR パイプライン側の作業。BC 残置仕様変更は OCR パイプライン側の話なので B 側で実装する。

### マージタイミング

要確認のまま。ocr_result フィールド追加だけなら A 側に影響なし見込み（A 側は BC モデルを触っていない前提）が、念のため A 側マージ時に確認する。

### ステータス

ステップ1（モデル変更）実装済み。ステップ2（UI改善）も実装済み（コミット 3ceacb6）。仕様書側の記述追加のみ残っている。

### 要確認事項の解消結果

| 当初の要確認事項 | 解消結果 |
|---|---|
| Contact のない BusinessCard の判別方法 | BC 側に ocr_result フィールドを新設して区別。当初の暫定案「Contact 側からの逆引き、モデル変更なし」は不採用。理由：(1)将来拡張に強い、(2)集計クエリが書きやすい、(3)ビュー・テンプレート側の判別ロジックが分散しない、(4) v1.4.x のデータ収集（OpenCV 改善のため）に有用 |
| ユーザーへの通知文言・通知方法 | 今回のスコープでは決めない。ステップ2の UI 実装でコード君に任せる方針（たんたん判断）→ ステップ2 で #14（ocr_result_badge）として実装済み |
| ケースA／ケースB の区別方法 | is_business_card 判定で機械的に区別。ケースA → is_business_card=False → ocr_result='not_business_card'、ケースB → is_business_card=True かつ has_minimum_info NG → ocr_result='insufficient_info'、ケースB派生（card 単位の OCR 例外）→ ocr_result='ocr_failed'（将来用、ステップ2以降） |

---

## #2 DuplicateCandidate の match_reason / matched_fields / MatchReason 削除

**対象仕様書**：統合最終版

### 項目名
DuplicateCandidate モデルから match_reason フィールド・matched_fields フィールド・MatchReason TextChoices を削除

### 現状仕様（仕様書の該当章節）

**第4章 §4.7 DuplicateCandidate**（行 387-407）

フィールド一覧に以下が含まれる：

| フィールド名 | 型 | 説明 |
|---|---|---|
| match_reason | CharField | email / mobile / name_strong / name_score 等 |
| matched_fields | JSONField | 一致したフィールド名の配列 |

**第14章 §14.4 モデル固有の TextChoices**（行 2477-2491）

| クラス | 値 |
|---|---|
| DuplicateCandidate.MatchReason | email / mobile / name_strong / name_score |

### 改訂内容

- §4.7 のフィールド一覧から `match_reason` と `matched_fields` を削除
- §14.4 の TextChoices 一覧から `DuplicateCandidate.MatchReason` を削除
- 本文中で MatchReason に言及している箇所があれば全て削除
- 別表 A.8 の DuplicateCandidate フィールド対照表からも削除

### 改訂理由

「ランクと一致フィールド表示で表現できるため別軸不要」（メモリ確定事項）。冗長なフィールドを削除して設計をシンプルにする。

### 影響範囲

- §4.7 DuplicateCandidate モデル定義
- §14.4 モデル固有の TextChoices
- 別表 A.8 DuplicateCandidate のフィールド対照表
- 本文中で match_reason / matched_fields / MatchReason に言及している箇所すべて

### 確定経緯

- v0.1.5 S-1 で確定
- コード側は A-1c で削除済み
- 参考：「Run_Generate_Duplicate_Candidates_詳細仕様書_v0_1_5.md」§0.7.3 に削除予定が記載されている

### 担当ブランチ

A 側（feature/v1.4.2-models）

### ステータス

実装済み（コード側削除完了）。仕様書側の修正のみ残っている。

---

## #3 ActionLog の配置先変更：duplicates → actionlogs として独立

**対象仕様書**：統合最終版

### 項目名
ActionLog を duplicates アプリから actionlogs アプリとして独立させる

### 現状仕様（仕様書の該当章節）

**第11章 §11.1 アプリケーション構成**（行 1440-1449）

| アプリ | 用途 |
|---|---|
| cards（既存） | BusinessCard、OriginalImage 関連 |
| persons（新規） | Person 関連 |
| contacts（新規） | Contact 関連 |
| duplicates（新規） | DuplicateCandidate、PersonMergeLog、**ActionLog** 関連 |

**第11章 §11.2 ディレクトリ構成**（行 1451-1476）

```
duplicates/models.py | DuplicateCandidate、PersonMergeLog、ActionLog モデル
```

**第4章 §4.10 ActionLog**（行 482-505）等で「duplicates アプリ内の ActionLog」前提の記述あり。

### 改訂内容

- §11.1 アプリケーション構成に **actionlogs（新規）** を追加し、5 アプリ構成にする
- §11.2 ディレクトリ構成で `actionlogs/models.py` に ActionLog モデルを移動
- duplicates アプリの ActionLog 関連記述を全て actionlogs アプリへ移行
- 本文中で「duplicates の ActionLog」と読める箇所を全て「actionlogs の ActionLog」に修正

### 改訂理由

ActionLog はモデル横断の汎用ログ（GenericForeignKey + JSONField）であり、duplicates アプリに閉じる責務ではないため、独立アプリとして配置する。

### 影響範囲

- §11.1 アプリケーション構成
- §11.2 ディレクトリ構成
- §4.10 ActionLog モデル定義（配置場所の記述）
- §4.11 ActionLog と PersonMergeLog の関係（配置場所の記述）
- 第10章 §10.9 ActionLog のモデルメソッド詳細
- 本文中の他の参照箇所
- 別表 A.1 アプリケーション一覧、A.2 モデル一覧

### 確定経緯

メモリで確定（「ActionLog は actionlogs として独立、仕様書側はオーパス君が修正予定」）

### 担当ブランチ

A 側（feature/v1.4.2-models）

### ステータス

実装中（コード君A 作業中）

---

## #4 ActionLog の diff フィールド削除

**対象仕様書**：統合最終版

### 項目名
ActionLog モデルから diff フィールドを削除

### 現状仕様（仕様書の該当章節）

**第4章 §4.10 ActionLog**（行 482-498）

| フィールド名 | 型 | 説明 |
|---|---|---|
| diff | JSONField (null=True) | 変更前後の値（更新時のみ） |

**§4.10.1 ActionLog のモデルメソッド**（行 500-504）

```
ActionLog.record(user, action, content_object=None, object_repr='', diff=None, extra=None, note='')
```

**別表 A.10 ActionLog のフィールド**（行 3112-3127）

| 日本語名 | コーディング名 |
|---|---|
| 差分 | diff |

### 改訂内容

- §4.10 のフィールド一覧から `diff` を削除
- §4.10.1 の `ActionLog.record(...)` シグネチャから `diff` 引数を削除
- 別表 A.10 から「差分 / diff」行を削除
- 本文中で diff フィールドに言及している箇所を全て削除（差分情報は #5 の data フィールドに格納する）

### 改訂理由

A-2e で確定。diff フィールドを廃止し、必要な追加情報は data（旧 extra）フィールドに統合する設計。

### 影響範囲

- §4.10 ActionLog モデル定義
- §4.10.1 ActionLog のモデルメソッド
- §10.9 ActionLog のモデルメソッド詳細
- 別表 A.10 ActionLog のフィールド対照表
- 本文中で diff に言及している箇所すべて

### 確定経緯

A-2e で確定。コード側はコミット a070c4c で削除済み。

### 担当ブランチ

A 側（feature/v1.4.2-models）

### ステータス

実装済み（コード側削除完了）。仕様書側の修正のみ残っている。

---

## #5 ActionLog の extra → data 改名

**対象仕様書**：統合最終版

### 項目名
ActionLog モデルの extra フィールドを data に改名

### 現状仕様（仕様書の該当章節）

**第4章 §4.10 ActionLog**（行 482-498）

| フィールド名 | 型 | 説明 |
|---|---|---|
| extra | JSONField (default=dict) | モデルごとの追加情報を自由に格納 |

**§4.10.1 ActionLog のモデルメソッド**（行 500-504）

```
ActionLog.record(user, action, content_object=None, object_repr='', diff=None, extra=None, note='')
```

**§4.11 ActionLog と PersonMergeLog の関係**（行 515）

> note | 操作履歴 + ユーザー入力（文字列） | extra に構造化データで格納 |

**別表 A.10 ActionLog のフィールド**（行 3112-3127）

| 日本語名 | コーディング名 |
|---|---|
| 追加情報 | extra |

### 改訂内容

- §4.10 のフィールド名を `extra` から `data` に変更
- §4.10.1 の `ActionLog.record(...)` シグネチャの `extra=None` を `data=None` に変更
- §4.11 の比較表で「extra に構造化データで格納」を「data に構造化データで格納」に修正
- 別表 A.10 の「追加情報 / extra」を「追加情報 / data」に修正
- 本文中で extra フィールドに言及している箇所を全て data に修正

### 改訂理由

A-2e で確定。extra という名前は Django Admin 等の標準フィールド名と混同しやすいため、data に改名して責務を明確化する。

### 影響範囲

- §4.10 ActionLog モデル定義
- §4.10.1 ActionLog のモデルメソッド
- §4.11 ActionLog と PersonMergeLog の関係
- §10.9 ActionLog のモデルメソッド詳細
- §12.10 重複チェックの実行ログ（ActionLog）
- 別表 A.10 ActionLog のフィールド対照表
- 本文中で extra に言及している箇所すべて

### 確定経緯

A-2e で確定。コード側はコミット a070c4c で改名済み。

### 担当ブランチ

A 側（feature/v1.4.2-models）

### ステータス

実装済み（コード側改名完了）。仕様書側の修正のみ残っている。

---

## #6 別表 C.1 の OriginalImage.STATUS_CHOICES 表示名を実装に合わせる

**対象仕様書**：統合最終版

### 項目名
OriginalImage.Status の表示名を実装の表記に合わせる

### 現状仕様（仕様書の該当章節）

**別表 C.1 OriginalImage.Status**（行 3156-3164）

| コード値 | 表示名 | 意味 |
|---|---|---|
| pending | 処理待ち | OCR 未実行 |
| processing | 処理中 | CAS で processing 遷移済み |
| extracted | 抽出済み | OCR 成功（部分失敗含む） |
| garbage | 名刺なし | cards 配列が空、または全 card が is_business_card=false |
| failed | 失敗 | OCR 全失敗 |

### 改訂内容

実装（cards/models.py）の表示名に合わせて、以下の 3 つを修正する。

| コード値 | 現状仕様の表示名 | 改訂後の表示名（実装に合わせる） |
|---|---|---|
| extracted | 抽出済み | **完了** |
| garbage | 名刺なし | **無効画像** |
| failed | 失敗 | **処理失敗** |

`pending`（処理待ち）と `processing`（処理中）は変更なし。

### 改訂理由

実装の表示名のほうが日本語として自然である（たんたん判断）。仕様書側を実装に合わせる方向で確定。

### 影響範囲

- 別表 C.1 OriginalImage.Status
- 本文中で各 Status の表示名に言及している箇所があれば該当箇所も修正

### 確定経緯

OpenCV 側（B 側サポート担当クロード君）からの伝言で確定。B 側サポートチャットでステップ 1C-2 完了報告時にたんたんが確認。

### 担当ブランチ

共通（実装側は既に該当の表示名で実装されているため、仕様書側の修正のみ）

### ステータス

実装側完了。仕様書側の修正のみ残っている。

---

## #7 §8.3 スコア表の具体値記載／§8.4 ランク判定の判定順序とAND関係明示／所属5フィールドの内訳明示

**対象仕様書**：統合最終版

### 項目名
スコア表とランク判定の記述精度向上

### 現状仕様（仕様書の該当章節）

**第8章 §8.3 スコア表**（行 663-667）

> 各フィールドの完全一致に対して、点数を加算する。confidence=high のフィールドのみが加算対象（low / medium は加算しない）。
>
> スコア表とランク閾値は config/constants.py で管理する。運用後にチューニング可能とするため、定数化された設計とする。

→ 具体的な点数値が仕様書本文に記載されていない。

**第8章 §8.4 ランク判定**（行 669-679）

| ランク | 必須条件 |
|---|---|
| exact_match | 200点以上 + 所属5フィールド両方一致 or 両方空 |
| possible_high | 200点以上（フルネーム不一致でもメール+携帯+所属で達成可能） |
| possible_mid | フルネーム一致 + メール or 携帯一致 |
| possible_low | 40〜119点 + フルネーム一致 |
| none | 上記いずれにも該当しない |

→ 判定順序（上から順に判定するか）と AND 関係（条件の組み合わせがどう評価されるか）が明示されていない。「所属5フィールド」の具体内訳も本表内では明示されていない。

### 改訂内容

**§8.3 スコア表の改訂**

config/constants.py で管理する具体値を、仕様書本文の表として明示記載する。例：

| フィールド | スコア（具体値） |
|---|---|
| email | XX |
| mobile | XX |
| full_name | XX |
| ... | ... |

（具体値はコード君A の確認事項1で確定した値を使用。確定値は実装側 config/constants.py を参照する）

**§8.4 ランク判定の改訂**

判定順序（上から順に判定する旨）と、各ランク内の条件の AND 関係を明示する。例：

> ランクは exact_match → possible_high → possible_mid → possible_low の順に上から評価し、最初に該当した条件のランクを採用する。各ランクの「必須条件」内に列挙された条件はすべて AND 関係である（すべて満たす必要がある）。

**所属5フィールドの内訳明示**

「所属5フィールド」の具体内訳を §8.4 内で明示する：**company / department / title / branch / address**。

### 改訂理由

コード君A の確認事項 1, 2, 4 で、現状の記述ではコーディング時に判断のブレが生じる箇所が確認された。仕様書としての記述精度を上げる。

### 影響範囲

- §8.3 スコア表
- §8.4 ランク判定
- 必要に応じて §8.10 重複検出の効率化アルゴリズム（絞り込み条件と整合性を取る）

### 確定経緯

コード君A 確認事項 1, 2, 4 で確定。

### 担当ブランチ

A 側（feature/v1.4.2-models）

### ステータス

確定（仕様書側の記述精度向上のみ残っている）

---

## #8 §8.7 代表メール判定のバリエーション記述拡張

**対象仕様書**：統合最終版

### 項目名
代表メール判定のバリエーション記述に「ドット」を追加

### 現状仕様（仕様書の該当章節）

**第8章 §8.7 代表メール判定**（行 739-747）

> メールアドレスのローカル部（@ より前）が以下のリストに該当する、または該当語の前後に**ハイフン・アンダースコア**が付くバリエーションに該当する場合、代表メールと判定する。
>
> 初期リスト：info / contact / support / sales / admin / office / mail / inquiry / help / service / shop / customer / reception
>
> バリエーション例：info-jp@、sales_team@、support2@ なども代表メール扱い。

### 改訂内容

§8.7 のバリエーション記述を以下に拡張：

> ……または該当語の前後に**ハイフン・アンダースコア・ドット**が付くバリエーションに該当する場合、代表メールと判定する。

バリエーション例にもドット区切りの例を追加（例：`info.jp@`、`sales.team@` など）。

### 改訂理由

実運用で `info.jp@example.com` のようなドット区切りのバリエーションが代表メールとして判定対象に含まれるべきと判断された（B 前半レビューで確定）。

### 影響範囲

- §8.7 代表メール判定
- §8.7.1 代表メール判定リストの追加運用（影響なし、変更不要の見込み）
- 実装側（DUPLICATE_GENERIC_EMAIL_LOCALPARTS の判定ロジック）が同じ仕様で実装されているか要確認

### 確定経緯

B 前半レビューで確定。

### 担当ブランチ

A 側（feature/v1.4.2-models）

### ステータス

確定（仕様書側の記述拡張のみ残っている）

---

## #9 BusinessCard モデルに ocr_result フィールド新規追加（5値TextChoices）

**対象仕様書**：統合最終版

### 項目名
BusinessCard モデルに OCR 処理結果を表す ocr_result フィールドを新規追加し、TextChoices で 5 値管理する

### 現状仕様（仕様書の該当章節）

**第4章 §4.3 BusinessCard**（行 218-232）

現状フィールド：id / original_image / card_image / card_index / orientation / created_at / updated_at の 7 フィールド。`ocr_result` フィールドは存在しない。

**第14章 §14.4 モデル固有の TextChoices**（行 2477-2491）

現状、BusinessCard 関連の TextChoices は `BusinessCard.Orientation` のみ。`BusinessCard.OcrResult` は存在しない。

**別表 C**（行 3154-3284）

C.1〜C.12 のセクションあり。`BusinessCard.OcrResult` セクションは存在しない。

**別表 A.4 BusinessCard のフィールド**（行 2998-3008）

現状 7 行。`ocr_result` 行は存在しない。

### 改訂内容

**(1) §4.3 BusinessCard のフィールド一覧に追加**

| フィールド名 | 型 | 説明 |
|---|---|---|
| ocr_result | CharField(20, choices) | OCR 処理結果（5 値、別表 C.X 参照） |

null=False、default 値は `'business_card'`。

**(2) §14.4 モデル固有の TextChoices 一覧に新規追加**

| クラス | 値 |
|---|---|
| BusinessCard.OcrResult | business_card / not_business_card / insufficient_info / ocr_failed / others |

**(3) 別表 C に BusinessCard.OcrResult セクションを新規追加**

セクション番号はオーパス君判断（C.2 と C.3 の間に挿入し、C.2 の Orientation の直後に置くなど、BusinessCard 関連を近接配置するのが自然）。

| コード値 | 表示名 | 意味 |
|---|---|---|
| business_card | 名刺 | OCR 成功、Contact 生成あり |
| not_business_card | 名刺ではない | is_business_card=False 判定 |
| insufficient_info | 情報不足 | has_minimum_info NG |
| ocr_failed | OCR 失敗 | card 単位の OCR 例外 |
| others | その他 | 上記いずれにも該当しない予期せぬケース |

**注**：`ocr_failed` と `others` は v1.4.x 時点では将来用の受け皿として定義のみ。コード上は存在するが、実装上のセット箇所はステップ2以降。

**(4) 別表 A.4 BusinessCard のフィールド対照表に追加**

| 日本語名 | コーディング名 |
|---|---|
| OCR 処理結果 | ocr_result |

### 改訂理由

ストック#1（has_minimum_info NG 時の BC 残置仕様）の実装に伴い、「Contact のない BusinessCard」の種別を明示的に表現する必要が生じた。

ocr_result フィールドによる明示的な表現を採用した理由：

1. 将来拡張に強い（OCR 失敗ケース ocr_failed や予期せぬ others ケースを最初から見据えた設計）
2. 集計クエリが書きやすい（`BusinessCard.objects.filter(ocr_result='insufficient_info')` で一発）
3. ビュー・テンプレート側の判別ロジックが分散しない
4. v1.4.x のデータ収集（OpenCV 改善のため）に有用

### 影響範囲

- §4.3 BusinessCard モデル定義（フィールド追加）
- §14.4 モデル固有の TextChoices 一覧（OcrResult 追加）
- 別表 C（新規セクション追加、セクション番号採番）
- 別表 A.4 BusinessCard フィールド対照表
- マイグレーション必要（BusinessCard テーブルに ocr_result カラム追加）
- §15.1 cards 処理ルール一覧（→ #10 で改訂）
- §4.3 / §4.4 BusinessCard と Contact の関係性（→ #11 で明文化）
- UI 反映（→ #14 ocr_result_badge カスタムタグで実装）

### 確定経緯

2026-05-07 たんたんと B 側サポートクロード君で方針確定。B 側サポートクロード君から仕様書改訂担当オーパス君へ報告書受領。

### 担当ブランチ

B 側（feature/opencv-improvement）

### マージタイミング

要確認のまま。ocr_result フィールド追加だけなら A 側に影響なし見込み（A 側は BC モデルを触っていない前提）が、念のため A 側マージ時に確認する。

### ステータス

実装済み（コミット 3ceacb6 で push 済み）。仕様書側の記述追加のみ残っている。

---

## #10 §15.1 cards 処理ルール一覧の改訂（C2／C3／C5）

**対象仕様書**：統合最終版

### 項目名
§15.1 cards 処理ルール一覧の C2／C3／C5 を、BC 残置仕様および現状実装に合わせて改訂する

### 現状仕様（仕様書の該当章節）

**第15章 §15.1 cards 処理ルール一覧**（行 2497-2507）

| ID | ケース | 処理 |
|---|---|---|
| C1 | cards 配列が空 | status=garbage、BusinessCard 作成なし |
| C2 | 全 card が is_business_card=false | status=garbage、BusinessCard 作成なし |
| C3 | 全 card が has_minimum_info で弾かれた | status=extracted（BusinessCard 0 件）、error_message に記録 |
| C4 | confidence=low/medium のフィールドあり | ContactFieldConfidence に記録(high は記録しない） |
| C5 | 切り抜き失敗（card_image=null） | has_minimum_info を満たせば BusinessCard 作成 |
| C6 | OCR 例外発生 | 当該 card のみ失敗扱い、他 card への波及なし |

### 改訂内容

**(1) C2 の改訂**

| ID | ケース | 処理（改訂後） |
|---|---|---|
| C2 | card が is_business_card=false | 当該 card は BusinessCard 作成（ocr_result='not_business_card'）、Contact / Person 作らず。全 card が is_business_card=false なら status=garbage、1件以上 BC が作られたら status=extracted |

**(2) C3 の改訂**

| ID | ケース | 処理（改訂後） |
|---|---|---|
| C3 | card が has_minimum_info で弾かれた | 当該 card は BusinessCard 作成（ocr_result='insufficient_info'）、Contact / Person 作らず。error_message には記録しない（silent skip）。全 card 弾かれの場合も status=extracted（BC は残る） |

**(3) C5 の改訂**

| ID | ケース | 処理（改訂後） |
|---|---|---|
| C5 | warp（透視変換）失敗 | 当該 card は detect_cards 段階で除外、BusinessCard 作成なし、OCR 呼び出しもなし |

**(4) C1／C4／C6 は変更なし**

- C1（cards 配列が空、status=garbage、BC 作成なし）：変更なし
- C4（confidence 記録）：変更なし
- C6（OCR 例外、当該 card のみ失敗扱い）：今回スコープでは変更なし。ステップ2以降で `ocr_result='ocr_failed'` の値をセットする実装が入る予定だが、ステップ1では既存の error_message 記録ルートを温存するため、仕様書 C6 記述も現時点では変更なし

### 改訂理由

**C2／C3 の改訂理由**：BC 残置仕様（#1）により、is_business_card=false および has_minimum_info NG のカードも BC として残す。Contact / Person は作らない。OCR 結果の種別は #9 で追加した ocr_result フィールドで表現する。

**C5 の改訂理由**：現状仕様書 C5「切り抜き失敗（card_image=null）→ has_minimum_info を満たせば BusinessCard 作成」は誤り。warp 失敗品を OCR に送る意味がなく（切り取れていない画像に OCR しても無駄）、現状実装は detect_cards 段階で warp 失敗品を除外している。実装が正しく、仕様書側を実装に合わせる方針。

### 影響範囲

- §15.1 cards 処理ルール一覧
- 別表 A.4 BusinessCard.card_image の説明補足（任意）：JPEG 保存失敗時のみ null になりうる旨の補足。warp 失敗時はそもそも BC が作られないので、card_image=null になる経路は save_card_image_tmp の JPEG 書き込み失敗のみ。補足するかはオーパス君判断（最終ドキュメント作成時に決定）。

### 確定経緯

2026-05-07 たんたんと B 側サポートクロード君で方針確定。B 側サポートクロード君から仕様書改訂担当オーパス君へ報告書受領。

### 担当ブランチ

B 側（feature/opencv-improvement）

### ステータス

実装済み（コミット 3ceacb6 で push 済み）。

---

## #11 §4.3 / §4.4 で「Contact のない BusinessCard が存在しうる」仕様を明文化

**対象仕様書**：統合最終版

### 項目名
§4.3 / §4.4 のどこかで、Contact のない BusinessCard が存在しうる仕様を明文化する

### 現状仕様（仕様書の該当章節）

**第4章 §4.3 BusinessCard**（行 218-232）および **§4.4 Contact**（行 241-）

現状、BC と Contact の関係性として「BC ↔ Contact が OneToOne 」前提で記述されており、Contact のない BC が存在する可能性は明示されていない。

### 改訂内容

§4.3 または §4.4 の適切な箇所（具体的位置はオーパス君判断）に、以下を明文化する：

- Contact のない BusinessCard が存在しうる（v1.4.x で追加された仕様）
- ocr_result が `'business_card'` でない BC は Contact を持たない
- ocr_result が `'business_card'` の BC は Contact を必ず持つ（OneToOne）
- BC 削除時の連鎖削除挙動：BC 削除 → Contact CASCADE → ContactFieldConfidence CASCADE → card_image FS 実体 post_delete（#17 関連）

具体的な記述場所（§4.3 末尾／§4.4.0 設計趣旨内／§4.3.1 の前など）と文言は最終ドキュメント作成時にオーパス君が判断する。

### 改訂理由

ストック#1（BC 残置仕様）と #9（ocr_result フィールド追加）により、BC と Contact の関係性が「常に 1:1」から「条件付きの 1:0..1」に変わる。仕様書本文で明文化しないと読者（実装者・レビュアー）が誤解する可能性が高い。

#17（CardDeleteView 新規追加）に伴い、BC 削除時の連鎖削除挙動も明文化対象に追加。

### 影響範囲

- §4.3 BusinessCard
- §4.4 Contact（特に §4.4.0 設計趣旨）
- §4.4.1 Contact のフィールド定義（business_card フィールドの説明）

### 確定経緯

2026-05-07 たんたんと B 側サポートクロード君で方針確定。
2026-05-08 #17 CardDeleteView 実装に伴い、削除カスケード仕様の明文化も追加対象として確認。

### 担当ブランチ

B 側（feature/opencv-improvement）

### ステータス

方針確定（仕様書側の記述追加のみ）

---

## #12 §4.2 detected_count の意味を「DBに保存されたBC数」と明文化

**対象仕様書**：統合最終版

### 項目名
OriginalImage.detected_count の定義を明文化する

### 現状仕様（仕様書の該当章節）

**第4章 §4.2 OriginalImage**（行 198）

| フィールド名 | 型 | 説明 |
|---|---|---|
| detected_count | IntegerField (default=0) | 検出された名刺数 |

→ 「検出された名刺数」とだけ書かれており、具体的に何をカウントするかが曖昧。

### 改訂内容

§4.2 の `detected_count` の説明を以下のように明文化する：

> | detected_count | IntegerField (default=0) | 検出された名刺数（OriginalImage に紐づく BusinessCard レコードの総数。ocr_result の値に関わらず、DB に保存された BC 全件をカウントする） |

または、§4.2 の表の下に補足文として記述する形式でも可。具体的な記述形式は最終ドキュメント作成時にオーパス君判断。

### 改訂理由

ストック#1／#9／#10 により、「検出された名刺」の意味が以下の 3 通りに解釈可能になった：

| 解釈 | 含まれる範囲 |
|---|---|
| (1) OpenCV 検出数 | OpenCV で切り取れた全カード |
| (2) BC 生成数 | DB に保存された BC 全部（NG ケース含む） |
| (3) OCR 成功数 | Contact が作られた BC のみ |

実装は (2) を採用しており、CLAUDE.md（リポジトリ内開発ガイド）の定義「DB に保存された BusinessCard 数」とも整合する。たんたん判断で (2) を正式な定義として確定（2026-05-07）。

仕様書本文の記述が曖昧だと、後の保守・改修時に解釈が揺れる可能性があるため、明文化する。

### 影響範囲

- §4.2 OriginalImage の detected_count フィールド説明
- 別表 A.3 OriginalImage の detected_count 説明（必要に応じて補足）
- 本文中で detected_count に言及している箇所（影響範囲は限定的）

### 確定経緯

2026-05-07 B 側サポートクロード君から問題提起。たんたん判断で (2) を採用と確定。

### 担当ブランチ

B 側（feature/opencv-improvement）

### ステータス

方針確定（仕様書側の記述明文化のみ）

---

## #13 DebugMask モデル新設（OpenCV検出パイプラインのデバッグ用マスク画像をDB管理）

**対象仕様書**：統合最終版

### 項目名
OpenCV 検出パイプラインのデバッグ用マスク画像（5種）を DB 管理するための DebugMask モデルを新設する

### 現状仕様（仕様書の該当章節）

**第4章 モデル定義**

現状、cards アプリのモデルは OriginalImage と BusinessCard のみ。DebugMask モデルは存在しない。

**第14章 §14.4 モデル固有の TextChoices**（行 2477-2491）

現状、DebugMask 関連の TextChoices は存在しない。

**別表 A.2 モデル一覧**（行 2969-2981）

現状、DebugMask は記載されていない。

**別表 A（A.3〜A.10）フィールド対照表**

現状、DebugMask のフィールド対照表は存在しない（新規追加が必要）。

**別表 C TextChoices 一覧**

現状、DebugMask 関連の TextChoices セクションは存在しない。

### 改訂内容

**(1) §4 モデル定義に新規節を追加**

§4.3 BusinessCard と §4.4 Contact の間、または §4.10 ActionLog 等の後ろに、以下の新規節を追加する。具体的位置はオーパス君判断（§4.3.1 直後の §4.3.2 として「BusinessCard 関連の補助モデル」扱いするか、§4 末尾に配置するか）。

**§4.X DebugMask（OpenCV デバッグ用マスク画像 DB）**

DebugMask は、OpenCV 検出パイプラインで生成されるデバッグ用マスク画像（5 種）を DB 管理するためのレコード。OriginalImage 1 件に対して最大 5 件（mask_type ごとに 1 件）の DebugMask が紐づく。

| フィールド名 | 型 | 説明 |
|---|---|---|
| id | UUIDField (PK) | プライマリキー |
| original_image | FK(OriginalImage, CASCADE, related_name='debug_masks') | 元画像への外部キー |
| mask_type | CharField(20, choices) | マスク種別（5 値、別表 C.X 参照） |
| mask_image | ImageField | マスク画像実体（保存先：`media/debug_masks/<original_id>/<mask_type>.png`） |
| metadata | JSONField (default=dict, null=False, blank=True) | マスク個別の属性（white_ratio 等） |
| created_at | DateTimeField | auto_now_add |

制約：UniqueConstraint(original_image, mask_type)。

**(2) §14.4 モデル固有の TextChoices 一覧に新規追加**

| クラス | 値 |
|---|---|
| DebugMask.MaskType | diff / edge / sat / or / closed |

**(3) 別表 C に DebugMask.MaskType セクションを新規追加**

セクション番号はオーパス君判断（既存 C.1〜C.12 の流れに沿って末尾追加、または BusinessCard 関連を近接配置する場合は中間挿入）。

| コード値 | 表示名 | 意味 |
|---|---|---|
| diff | 差分マスク | 差分検出マスク |
| edge | エッジマスク | エッジ検出マスク |
| sat | 彩度マスク | 彩度ベースのマスク |
| or | OR 合成マスク | 複数マスクの OR 合成結果 |
| closed | クローズマスク | モルフォロジー処理後のマスク |

（各表示名・意味の文言は実装の TextChoices ラベルに合わせて最終ドキュメント作成時にオーパス君が確認・調整）

**(4) 別表 A.2 モデル一覧に追加**

| アプリ | モデル | 用途 |
|---|---|---|
| cards | DebugMask | OpenCV デバッグ用マスク画像 |

**(5) 別表 A.X DebugMask のフィールド対照表を新規追加**

| 日本語名 | コーディング名 |
|---|---|
| プライマリキー | id |
| 元画像 | original_image |
| マスク種別 | mask_type |
| マスク画像 | mask_image |
| メタデータ | metadata |
| 作成日時 | created_at |

セクション番号は A.10（ActionLog）の後ろに A.11 として追加し、既存の A.11（主要なプロパティ・属性）を A.12 に繰り下げ。

**(6) §11.2 ディレクトリ構成への影響なし**

DebugMask は cards アプリ配下のため、§11.2 のディレクトリ構成記述に新規追加は不要。

**(7) post_delete シグナルによる FS 実体自動削除の言及**

§4 末尾、または §11 アプリケーション設計の補足節として、以下の設計思想を簡潔に明記する（具体的位置はオーパス君判断）：

> DB が 1 次ソース、FS 実体は DB レコードに紐付いた従属物として扱う。OriginalImage / BusinessCard / DebugMask それぞれに post_delete シグナルが定義され、レコード削除時に対応する FS 実体（image_file / card_image / mask_image）が自動削除される。

### 改訂理由

開発フェーズで OpenCV チューニングを繰り返す前提で、開発環境の整合性を確立する設計改修。

現状の問題：
- mask 画像（`media/debug_cache/<id>/mask_*.png`）が DB と独立して FS 上に存在
- `_build_mask_urls` が DB の debug_json を見ず、FS を `f.exists()` で直接チェック
- `db.sqlite3` を削除しても media 配下の FS 実体が残り、開発環境の完全リセットができない

設計思想：
- **DB が 1 次ソース、FS 実体は DB レコードに紐付いた従属物**
- DB レコード削除＝関連 FS 実体も自動削除
- 開発環境完全リセット手順「`db.sqlite3` 削除＋ migrate」だけで完結する状態を目指す

DebugMask モデル化により：
- mask_white_ratios（旧 debug_json のキー）が DebugMask.metadata に整理され、責務が明確化
- save_debug_data / clear_debug_cache / _build_mask_urls の責務が DB レコード経由に統一
- post_delete シグナルで FS 実体の自動クリーンアップが可能になる

### 影響範囲

- §4 モデル定義に新規節追加（DebugMask）
- §14.4 モデル固有の TextChoices 一覧（DebugMask.MaskType 追加）
- 別表 A.2 モデル一覧
- 別表 A.X DebugMask フィールド対照表（新規追加、セクション番号採番）
- 別表 C に DebugMask.MaskType セクション追加
- §4 末尾または §11 補足節に「DB 1 次ソース、FS 従属物」設計思想の明記
- マイグレーション必要（マイグレーション 0008：DebugMask モデル追加）
- 関連サービス層改修（save_debug_data / clear_debug_cache / _build_mask_urls）
- post_delete シグナル新規実装（cards/signals.py）

### 確定経緯

2026-05-07 たんたんと B 側サポートクロード君で設計議論。最終確定スコープは以下：

| 項目 | 内容 |
|---|---|
| 新規モデル | DebugMask（OriginalImage への ForeignKey、mask_type、mask_image、metadata、created_at） |
| マイグレーション | 0008（DebugMask 追加 + debug_json から mask_white_ratios 削除） |
| FS 保存先 | `media/debug_masks/<original_id>/<mask_type>.png` |
| save_debug_data 改修 | DebugMask レコード作成、metadata に white_ratio 格納、debug_json から mask_white_ratios 削除 |
| clear_debug_cache 改修 | debug_json=None + DebugMask 一括削除（CASCADE ではなく queryset.delete() で post_delete 発火） |
| _build_mask_urls 改修 | DebugMask レコードを引いて URL 返却（FS 直読み廃止） |
| post_delete シグナル | OriginalImage / BusinessCard / DebugMask それぞれで FS 実体削除 |
| テンプレート側 | mask_white_ratios 参照箇所を DebugMask.metadata 経由に変更 |
| スコープ外 | reconcile 拡張、完全リセット管理コマンド、ステップ2（UI改善） |

### 担当ブランチ

B 側（feature/opencv-improvement）

### マージタイミング

要確認。DebugMask モデルは cards アプリ配下のため、A 側との競合は限定的だが、別表 A への追記やセクション番号採番で衝突する可能性があるため、マージ時に整合性を確認する。

### ステータス

実装済み（コミット 3ceacb6 で push 済み）。

### 関連項目

本変更に伴い、OriginalImage.debug_json から `mask_white_ratios` キーを削除する変更も発生する。これは「マージ後反映候補 B-1」の延長線上の話として B-1 に補足記載する。

---

## #14 ocr_result_badge カスタムタグの新規追加

**対象仕様書**：統合最終版

### 項目名
BusinessCard の ocr_result 値に応じてバッジを表示するカスタムタグ ocr_result_badge を新規追加

### 現状仕様（仕様書の該当章節）

**第11章 §11.8.1 カスタムタグ一覧**（行 1904-1912）

現状リスト：card_image / original_image_thumbnail / json_tree / confidence / contact_confidence の 5 種。`ocr_result_badge` は存在しない。

**第11章 §11.8.2 カスタムタグの詳細**（行 1914-1943）

現状、`ocr_result_badge` の記述なし。

**第11章 §11.2 ディレクトリ構成**（行 1451-1476）

現状、cards/templatetags 配下のファイルとして `back_tags.py` と `ui_tags.py` が記載されているが、`ui_tags.py` の中身カスタムタグはまだ未実装の構想段階だった。

### 改訂内容

**(1) §11.8.1 カスタムタグ一覧に追加**

| タグ | 引数 | 用途 |
|---|---|---|
| `{% ocr_result_badge bc %}` | BusinessCard インスタンス | OCR 処理結果のバッジ表示 |

§11.8 冒頭の「カスタムタグ 5 種」を「カスタムタグ 6 種」に修正。

**(2) §11.8.2 カスタムタグの詳細に新規節を追加**

```
#### `{% ocr_result_badge bc %}`

BusinessCard の ocr_result 値に応じて、業務上の警戒度を表すバッジを表示する。

引数：BusinessCard インスタンス（None 時は空文字を返す防御あり）

出力ルール：

| ocr_result 値 | バッジクラス | 表示テキスト |
|---|---|---|
| business_card | （バッジなし、空文字を返す） | - |
| not_business_card | app-status-badge--muted | 名刺ではない |
| insufficient_info | app-status-badge--warning | 情報不足 |
| ocr_failed | app-status-badge--error | OCR失敗 |
| others | app-status-badge--muted | その他 |

ラベルは bc.get_ocr_result_display() 経由で TextChoices 定義から取得する（仕様書で表示名が変わっても自動追従）。

バッジ色の設計思想：「業務上の警戒度」で割り当て。muted=想定外だが警戒度低、warning=ユーザーに撮り直しを促す系、error=明確な失敗。

not_business_card と others が同じ muted になっている点は、現時点で others がセットされる経路自体がない（v1.4.x 時点では将来用受け皿のみ）ため、実用上の問題なし。実際に others が出る経路ができたタイミングで色分けを再検討する。
```

**(3) §11.2 ディレクトリ構成の補足**

`cards/templatetags/__init__.py` と `cards/templatetags/ui_tags.py` が新規ファイルとして実装された旨を明記する（既存記述では `ui_tags.py` は構想段階の記載）。

### 改訂理由

#9 で BusinessCard.OcrResult を 5 値で導入したが、UI 上で出し分けるための共通基盤が必要。仕様書 §11.8 のカスタムタグ群（card_image / confidence / contact_confidence 等）は将来追加予定で現状未実装、今回が cards/templatetags/ui_tags.py の最初のタグ実装となる。

各画面（名刺一覧 #15、名刺詳細 sibling_cards #16、元画像詳細セクション7 #18）から共通利用するため、カスタムタグ化が責務分離として妥当。

### 影響範囲

- §11.8.1 カスタムタグ一覧（5 種 → 6 種）
- §11.8.2 カスタムタグの詳細に ocr_result_badge セクション新規追加
- §11.8 冒頭の「カスタムタグ 5 種」記述を「6 種」に修正
- §11.2 ディレクトリ構成：cards/templatetags/__init__.py と cards/templatetags/ui_tags.py を新規ファイルとして記載

### 確定経緯

2026-05-08 たんたんと B 側サポートクロード君で議論。「仕様書 §11.8 にカスタムタグ群の構想があるなら、ocr_result の表示もカスタムタグ化するのが筋」と方針確定。たんたんから「前任クロード君もカスタムタグのこと知らなかったと思う。今回の ocr_result 系もカスタムタグでいい」との確定。

### 担当ブランチ

B 側（feature/opencv-improvement）

### ステータス

実装済み（コミット 3ceacb6 で push 済み）。仕様書側の記述追加のみ残っている。

---

## #15 CardListView に ocr_result フィルタ機能追加

**対象仕様書**：統合最終版

### 項目名
名刺一覧画面（CardListView）に ocr_result によるステータスフィルタ機能を追加

### 現状仕様（仕様書の該当章節）

**第13章 サービス層またはビュー層**

CardListView の検索条件記述があれば該当箇所。ステップ1で「ocr_result='business_card' フィルタ追加」（ハードコード絞り込み）として実装済みだったが、ステップ2でフィルタ機能として正規化された。仕様書本体への反映は未着手。

### 改訂内容

**(1) CardListView の検索フォームに「ステータス（OCR結果）」欄を追加**

- 5 値のチェックボックス（名刺 / 名刺ではない / 情報不足 / OCR失敗 / その他）
- URL クエリパラメータ：`ocr_result`（複数値対応、checkbox の name="ocr_result"）
- 初回アクセス時（パラメータ皆無）：「名刺」のみがチェック ON
- パラメータ 0 件 → 内部的に `business_card` のみへフィルタ
- パラメータ 1 件以上 → `ocr_result__in=[...]` で絞り込み

**(2) 一覧表示の各行に ocr_result_badge を表示（business_card 以外のとき）**

フィルタで「無効名刺だけ」を選んだとき、表示上それが何系の無効か分からないと使い物にならないため、サムネ列内に縦並びでバッジを追加。

**(3) BackNavigator の保持パラメータに ocr_result を追加**

詳細画面遷移時に検索条件が保持されるよう、`ocr_result` パラメータを BackNavigator のクエリキーに含める。

### 改訂理由

ステップ1で実装した「ocr_result='business_card' のみ表示」のハードコード絞り込みでは、OCR非成功BC（TENPOUP のような名刺ではないBC等）を一覧から探せない問題が動作確認時に発覚。ユーザーが必要に応じて非成功BCも閲覧できるよう、フィルタ機能として正規化した。

引き継ぎメモには「名刺一覧画面は変更なし」と記載されていたが、たんたん判断でスコープを拡張して対応した。

### 影響範囲

- CardListView の検索パラメータ仕様
- 第13章 関連節（サービス層またはビュー層の記述）
- templates/cards/card_list.html の検索フォームと一覧表示
- BackNavigator の保持パラメータ一覧（仕様書記載があれば該当箇所）

### 確定経緯

2026-05-08 動作確認中にたんたんから「TENPOUPの名刺詳細画面が出てこないので、一覧画面で検索できるようにと、元画像詳細画面で名刺詳細画面へのリンク作成を指示した」との連絡。コード君B が実装、動作確認 OK。

### 担当ブランチ

B 側（feature/opencv-improvement）

### ステータス

実装済み(コミット 3ceacb6 で push 済み）。仕様書側の記述追加のみ残っている。

---

## #16 CardDetailView の sibling_cards を通常表示エリアに移動

**対象仕様書**：統合最終版

### 項目名
名刺詳細画面の sibling_cards を {% if debug %} の中から外に出し、通常表示エリアの独立セクション「同じ画像に含まれる他の名刺」として表示するように変更

### 現状仕様（仕様書の該当章節）

**第13章 CardDetailView 関連節**、または **templates/cards/card_detail.html のテンプレート構成記述**

現状、sibling_cards は CardDetailView.get_context_data() で取得しているが、テンプレート上では `{% if debug %}` 内の `<details>` セクション「開発用情報」配下に表示されていた。つまり通常運用時は見えない状態。

### 改訂内容

**(1) sibling_cards の表示位置を {% if debug %} の外に出す**

- 配置場所：Contact 情報を表示している大カラムの下、独立セクションとして
- セクション見出し：「同じ画像に含まれる他の名刺」
- レイアウト：既存の app-list-card を踏襲

**(2) 各 sibling BC に ocr_result_badge カスタムタグを表示**

- business_card のときはバッジなし
- それ以外のときはバッジ表示

**(3) sibling_cards の絞り込みは現状維持**

- `BusinessCard.objects.filter(original_image=self.object.original_image).exclude(pk=self.object.pk)` で主役 BC を除外、OCR 非成功 BC も含めて全 BC 表示
- 「他の」名刺なので主役 BC 自身は含めない（セクション名とも整合）

### 改訂理由

ステップ2 の引き継ぎメモで「sibling_cards に全 BC 表示（OCR 非成功 BC 含む）+ ocr_result バッジ表示」と指示があった。OCR 非成功 BC を全部見せる目的が「ユーザーが他の BC も見たい」場合、開発用情報セクションの中だと通常運用で見えない。本来の利用者が触る場所に出す必要がある。

### 影響範囲

- 第13章 CardDetailView 関連節
- templates/cards/card_detail.html の構成記述
- sibling_cards の表示仕様

### 確定経緯

2026-05-08 たんたんと B 側サポートクロード君で論点整理。クロード君推奨「(A) 外に出して通常表示にする」をたんたんが採用。

### 担当ブランチ

B 側（feature/opencv-improvement）

### ステータス

実装済み（コミット 3ceacb6 で push 済み）。仕様書側の記述追加のみ残っている。

---

## #17 CardDeleteView の新規追加（名刺ハード削除機能）

**対象仕様書**：統合最終版

### 項目名
名刺詳細画面に削除ボタンを追加し、BusinessCard をハード削除する CardDeleteView を新規追加

### 現状仕様（仕様書の該当章節）

**第13章 cards アプリのビュー層**、**§11.2 ディレクトリ構成**、**URL ルーティング記述**

現状、cards/urls.py には card_list / card_upload / card_detail の 3 URL のみ。削除用 URL とビューは存在しない。

### 改訂内容

**(1) cards/urls.py に削除 URL を追加**

- パス：`<uuid:pk>/delete/`
- name：`card_delete`
- 完全 URL：`/cards/<uuid:pk>/delete/`

**(2) cards/views.py に CardDeleteView を新規追加**

- POST 専用ビュー（GET / その他は 405）
- 認証ガード：`get_object_or_404(BusinessCard, pk=pk, original_image__user=user)`
- 削除内容：`bc.delete()` を呼ぶだけ
- 削除後のリダイレクト先：`originals:original_detail`（削除前に `bc.original_image_id` を控えておく）

**処理フロー（補足）**：

1. `bc.delete()` 実行
   - Django ORM が BusinessCard レコードを削除
   - CASCADE で Contact（OneToOneField）が連鎖削除
   - CASCADE で ContactFieldConfidence（Contact への ForeignKey）が連鎖削除
   - post_delete シグナルで card_image の FS 実体が削除（cards/signals.py）
2. `redirect("originals:original_detail", pk=original_image_id)` でブラウザに 302 を返す
3. ブラウザは `/originals/<uuid>/` へ GET リクエスト
4. OriginalDetailView がレンダリング → 削除した BC が消えた状態の元画像詳細画面が表示される

OriginalImage.raw_json には cards 配列が残っているので、その元画像にアップロード時の OCR 全カード情報は失われずに残る（不変ルール v1.2.1）。

post() メソッドはリダイレクトを返して即終了する。

**(3) 名刺詳細画面（card_detail.html）の右下「戻る」ボタン近くに削除ボタンを追加**

- フォーム形式（POST、CSRF トークン付き）
- ボタン外観：app-btn--secondary、app-icon--delete アイコン使用
- 削除確認ダイアログ：`window.confirm('この名刺を削除します。よろしいですか？')`
- 既存の RecalcDebugView の `js-recalc-debug-form` パターンを踏襲

### 改訂理由

OCR 非成功 BC（特に not_business_card のように名刺としては不要な BC）をユーザーが画面から削除できるようにするため。raw_json は OriginalImage 側に温存されるので、後から OCR 結果の生データを参照することは可能（後追い検証用）。

不変ルール v1.2.1（OriginalImage.raw_json は OCR 全カード情報を温存）と整合する設計。

### 影響範囲

- 第13章 cards ビュー層に CardDeleteView 節を新規追加
- §11.2 ディレクトリ構成：cards/views.py（既存）、cards/urls.py（既存）に追記
- URL ルーティング一覧（あれば該当箇所）
- 削除カスケード仕様の明記：BC 削除 → Contact CASCADE → ContactFieldConfidence CASCADE → card_image FS 実体 post_delete（→ #11 で BC ↔ Contact 関係性明文化と統合）

### 確定経緯

2026-05-08 たんたんと B 側サポートクロード君で論点整理。仕様：

- リダイレクト先：元画像詳細
- 確認ダイアログ：必要
- Contact CASCADE で連動削除：仕様通り
- raw_json 温存：cards 配列の該当要素も残る

動作確認で TENPOUP（not_business_card）を削除して全項目 OK 確認済み。

### 担当ブランチ

B 側（feature/opencv-improvement）

### ステータス

実装済み（コミット 3ceacb6 で push 済み）。仕様書側の記述追加のみ残っている。

---

## #18 OriginalDetailView セクション7「検出された名刺」の改修

**対象仕様書**：統合最終版

### 項目名
元画像詳細画面（OriginalDetailView）のセクション7「検出された名刺」テーブルに、操作列・サムネイル列・OCR結果列を追加

### 現状仕様（仕様書の該当章節）

**第13章 OriginalDetailView 関連節**、または **templates/cards/original_detail.html のテンプレート構成記述**

現状のセクション7 テーブル列：名刺ID / card_index / 向き / 切り抜き画像（あり/なし） / 作成日時 の 5 列。サムネイル表示なし、名刺詳細へのリンクなし。

### 改訂内容

**(1) テーブル左端に「操作」列を追加**

- 詳細ボタン（app-btn--secondary--sm）で名刺詳細画面（cards:card_detail）に遷移
- BackNavigator の append_back_url 経由でリンク生成

**(2) 「操作」列の右隣に「サムネイル」列を追加**

- card.card_image があればサムネイル表示（max-height:56px、max-width:120px）
- なければ「画像なし」表示

**(3) 「向き」と「切り抜き画像」の間に「OCR結果」列を追加**

- ocr_result_badge カスタムタグで表示

**(4) 結果として列構成**：操作 / サムネイル / 名刺ID / card_index / 向き / OCR結果 / 切り抜き画像 / 作成日時 の 8 列

- empty 行の colspan を 5 → 8 に修正

**(5) スコープ外として残した課題**：「切り抜き画像（あり/なし）」列はサムネ列追加で冗長化したが、今回は触らず維持(→ 後述「将来検討メモ」参照）

### 改訂理由

動作確認時に「TENPOUP の名刺詳細画面が出てこない」問題が発覚（一覧フィルタで弾かれていた）。元画像詳細から直接名刺詳細に遷移できる導線がなかった。サムネ表示も視認性向上のため追加。

### 影響範囲

- 第13章 OriginalDetailView 関連節
- templates/cards/original_detail.html のセクション7 テーブル構成記述
- 「切り抜き画像」列の将来的な削除検討メモ（→ 後述「将来検討メモ」参照）

### 確定経緯

2026-05-08 動作確認中にたんたんから追加対応依頼。コード君B が実装、動作確認 OK。

### 担当ブランチ

B 側（feature/opencv-improvement）

### ステータス

実装済み（コミット 3ceacb6 で push 済み）。仕様書側の記述追加のみ残っている。

---

## #19 X-3 ランナバグ修正に伴う v0.1.5 詳細仕様書の改訂

**対象仕様書**：v0.1.5 詳細

### 項目名
Run_Generate_Duplicate_Candidates_詳細仕様書 v0.1.5 の §3.6 / §3.6.1 / §3.4（処理フロー）/ §5.3 / §5.4.1 を、X-3 ランナバグ修正で確定した「事前フィルタロジック」を反映する形に改訂する

### 現状仕様（仕様書の該当章節）

**v0.1.5 §3.6 partial unique constraint との関係**

> ただし、本仕様の運用では以下の理由により、この衝突は実運用上発生しない想定とする。
> - 12.7 の処理が Contact 編集時に必ず発火し、関連 pending を invalidated 化する
> - マージ実行時の recover 処理（rev5 7.3）が同 Person ペアの pending を整理する
> - duplicate_checked_at = NULL の Contact のみが本関数の処理対象となるため、過去 pending の存在は cron 起動時点では想定されない
> - 上位関数 Run_Generate_Duplicate_Candidates の 4.4.3 で 3 段の再チェックを行うため、ロック取得から processing までの間の状態変化による競合経路は排除される

→ この前提は「12.7 / recover が事前に整理する経路でのみ pending DC が作られる」想定だが、新規 OCR 取り込みで同一名刺ペアの両側が同一 cron バッチに入るケースや、テストデータ生成スクリプト経由の流入では成立しない。実装で X-3 修正により事前フィルタを追加したため、仕様書側もこれに合わせる必要がある。

**v0.1.5 §3.3 generate_duplicate_candidates_for_contact の処理フロー**

現状の手順 (3) と (4) の間に、事前フィルタの記述がない。

**v0.1.5 §5.4.1 なぜ履歴参照判断を generate 側に集約するか**

現状の責務分担記述では「履歴参照判断は different_person 除外と group_id 発行のみ」と読める。X-3 修正で「既存 pending DC の存在チェック」も generate 側の履歴参照判断として加わったため、責務一覧に明記が必要。

### 改訂内容

**(1) §3.6 の書き換え**

「実運用では発生しない想定」の記述を削除し、「事前フィルタで衝突自体を回避する」方針に書き換える。具体的には：

- 12.7 / recover が事前に整理する経路で「衝突を未然に防ぐ」想定だった部分を削除
- 代わりに「同一 cron バッチ内で同 Person ペアの両側が処理対象になるケース（新規 OCR 取り込み・テストデータ流入等）でも、事前フィルタで衝突を回避する」と明記
- 4.4.3 の 3 段再チェックは引き続き有効、これは事前フィルタとは別レイヤーの防御である旨を明記

**(2) §3.6.1 の書き換え**

現状「IntegrityError 発生時の挙動（v0.1.2 で確定）」として記述されているが、事前フィルタ追加により IntegrityError は発生しない想定に変わる。記述を以下の方向で書き換え：

- ignore_conflicts=True を付けない方針は維持（v0.1.2 確定）
- 万一 IntegrityError が発生した場合は、事前フィルタの実装漏れまたは race condition の証拠であり、当該 contact のトランザクションをロールバックして上位ループに伝播させる（既存の §1.3 / §4.5 と整合）

**(3) §3.3 処理フローへの事前フィルタステップ追加**

現状の手順を以下の構成に変更：

- 現状の手順 (3) と (4) の間に、新たに **手順 (3.5) 事前フィルタ** を挿入
- 手順 (3.5) の内容：「contact.person と既に pending DC として組まれている Person ID 集合を、person_a / person_b 別々の 2 クエリで取得し、その集合に含まれる候補をスキップする」
- SQLite 3.51.2 planner bug 回避のため Q(person_a=p) | Q(person_b=p) は使わない旨を補足
- スキップ判定は person_id レベルで行う旨を明記
- 手順番号は (3.5) として挿入するか、全手順番号を振り直すかはオーパス君判断

**(4) §3.4 DuplicateCandidate インスタンスの各フィールドの値**

特に変更なし。事前フィルタで除外された候補は DuplicateCandidate インスタンスを構築する手順自体に到達しないため、フィールド値定義は影響を受けない。

**(5) §5.3.2 cron 新規生成時の group_id 発行ロジック**

「事前フィルタで生き残った候補のみに group_id 発行を適用する」旨を補足。フィルタで除外された候補には group_id 発行を行わない（DuplicateCandidate インスタンス自体を作らないため自然にそうなる）が、明文化しておくと読みやすい。

**(6) §5.4.1 履歴参照判断の責務一覧に追記**

現状「履歴参照判断（different_person 除外、group_id 発行）はすべて generate_duplicate_candidates_for_contact 側で実施し」と書かれている部分を、以下に拡張：

- 履歴参照判断（different_person 除外、group_id 発行、**既存 pending DC の存在チェック**）はすべて generate_duplicate_candidates_for_contact 側で実施する
- 「DB 履歴を見る判断」の責務が generate 側に一極集中する設計趣旨は維持

**(7) §6.2 確認観点への追加**

確認観点 #8 として「同 cron バッチ内で同 Person ペアの両側が処理対象になっても IntegrityError なしで完走する」観点を追加。テストデータ生成スクリプト dev_create_test_contact_data --reset → check_duplicates --limit 100 で再現可能な手順として記載。

### 改訂理由

X-3 ランナバグ修正（コミット 4cd1c3e）で、partial unique constraint 違反を事前フィルタで回避する実装が確定した。仕様書側の前提記述（「実運用では発生しない想定」）と実装が乖離している状態を解消する。

### 影響範囲

Run_Generate_Duplicate_Candidates_詳細仕様書 v0.1.5：
- §3.3 処理フロー
- §3.4 DuplicateCandidate インスタンスの各フィールドの値（変更なしの確認）
- §3.6 partial unique constraint との関係
- §3.6.1 IntegrityError 発生時の挙動
- §5.3.2 cron 新規生成時の group_id 発行ロジック
- §5.4.1 履歴参照判断の責務一覧
- §6.2 確認観点

統合最終版への波及：なし（v0.1.5 は補助文書、統合最終版は X-3 関連の詳細を含まない）。

### 確定経緯

2026-05-08 サポート担当クロード君（A 側、本流）の論点整理 → たんたん確定 → コード君A 実装（コミット 4cd1c3e）→ 一次レビュー OK → push 完了。

たんたんが論点として確定した方針：
- 修正方針：候補A（generate 側で bulk_create 前にフィルタ）
- 論点1：バッチ内インメモリ重複管理は不要、各 contact の atomic コミット後の DB 状態を読めば十分
- 論点2：案2a（既存 pending DC があるペアはスキップ、先勝ち）
- 論点3：score / rank の不一致は先勝ちで済ませる（§12.8.4 補助レコードに完璧な整合性を求めず UX 優先と整合）
- 論点4：duplicate_checked_at の更新ロジックは変更なし（候補ゼロでも処理完了として更新）

### 担当ブランチ

A 側（feature/v1.4.2-models）

### マージタイミング

仕様書側の修正のみ。実装側は既に push 済（コミット 4cd1c3e）。

### ステータス

実装済み（コード側 push 完了）。仕様書側の修正のみ残っている。

---

## #20 Contact 詳細画面 AJAX 化に伴う設計方針大転換（廃止確定 3 項目を包含）

**対象仕様書**：統合最終版

### 項目名
Contact 詳細画面 AJAX 化に伴うマージ系処理の設計方針大転換。Execute_Merge_with_Updates サービス・マージ画面のフィールド値編集機能・MergeForm の値修正関連メソッドを廃止する。

### 設計方針大転換の概要

#### 旧設計（v1.4.2 統合最終版時点）

confidence の確認とフィールド値修正を、**マージ画面（17 番）の中で行う**設計。Execute_Merge_with_Updates サービスがマージ + フィールド修正 + confidence 部分 confirmed 化を担い、merge_reason='same_card' のときだけ特殊扱い（サバイブ側 primary を直接更新、新規 Contact 作らない）。

#### 新設計

confidence の確認とフィールド値修正を、**Contact 詳細画面（11 番 ContactDetailView）で AJAX で行う**設計に転換する。マージ画面はマージ実行と confidence 確認のみを担い、フィールド値の修正は行わない。値修正は Contact 詳細画面の AJAX 化により事前に済ませる流れとする。

#### 設計思想

「Contact = ある時点のスナップショット、修正ではなく履歴として積み上がる」という業務モデルを徹底する。マージは Person 単位の統合のみを担い、Contact のフィールド値には触らない。フィールド単位の取り込み（パターン 5：マージド側にしかない情報の取り込み）は、ほぼ発生しない例外パターンとして割り切り、フィールド単位の修正処理を持たない。

note の累積については、過去 Contact が inactive で残ることを利用し、履歴表示 UI（Contact 詳細画面の延長）で対応する。マージ処理内で note アペンドはしない。

#### 影響範囲（俯瞰）

- マージ処理：Execute_Merge_with_Updates の廃止、§9.4.5 same_card 修正ありの特殊扱いの廃止
- マージ画面：3 カラム構造の変更、フィールド値編集 UI の廃止、low/mid 修正・確認 UI の廃止（confidence 確認のみ残す）、値違いコピーボタン廃止、notes 結合廃止
- MergeForm：get_update_contact / confirmed_field_names / has_field_updates の廃止、責務をマージ判定情報のみに縮小
- Contact 詳細画面：AJAX で値修正・confidence 確認を行う仕様の追加（詳細は D-3 実装後に別途ストック追記）

---

### 廃止項目 1：Execute_Merge_with_Updates サービスの廃止

**現状仕様の該当章節**：§9.3.1 / §9.4.4 / §9.4.5、§13.4 サービス層

**廃止内容**：
- Execute_Merge_with_Updates サービス関数の廃止
- フィールド修正の有無による Execute_Merge_Only / Execute_Merge_with_Updates の分岐廃止（§9.4.4）
- merge_reason='same_card' かつ修正ありの特殊扱いの廃止（§9.4.5）
- duplicates/services/merge_executor.py からの実装削除

**廃止理由**：
Contact 詳細画面で confidence 確認・値修正を AJAX で行う新設計に伴い、マージ処理内でフィールド値修正を行う必要がなくなったため。マージ画面に来る前に Contact 詳細画面で値修正済みである前提とする。

---

### 廃止項目 2：マージ画面のフィールド値編集機能の廃止

**現状仕様の該当章節**：§11.5.5 / §11.5.6 / §11.5.7

**廃止内容**：
- マージ画面の中央カラム（マージ後の Contact 編集フォーム）の編集機能廃止
- low/mid フィールドの修正・確認 UI の廃止（confidence 確認 UI のみ残す）
- 値違い時の「→」コピーボタンの廃止
- notes フィールドの結合機能の廃止
- 3 カラム構造の見直し（編集中央カラムを廃止し、左右の比較表示 + 中央の判定情報入力に変更、詳細は D-3 完了後にストック追記）
- 「確認必須フィールドがすべて確認済みになるまでマージ確定ボタンは非活性」のロジックは confidence 確認のみで継続（フィールド値修正前提を外す）

**廃止理由**：
Contact 詳細画面で confidence 確認・値修正を AJAX で行う新設計に伴い、マージ画面で値修正を行う必要がなくなったため。マージ画面では情報の見比べと判定（同一人物 / 別人）に専念する。

---

### 廃止項目 3：MergeForm の値修正関連メソッド・フィールドの廃止

**現状仕様の該当章節**：§11.6.2

**廃止内容**：
- get_update_contact() メソッドの廃止
- confirmed_field_names() メソッドの廃止
- has_field_updates() メソッドの廃止
- 値違い確認の選択肢（左カラム採用 / 右カラム採用 / 手入力）の廃止
- 中央フォームの初期値ロジックの廃止

**継承関係の変更**：
MergeForm は ContactBaseForm を継承する必要がなくなる。マージ判定情報（review_status / surviving_person / merged_person 選択 / review_result / merge_reason / review_note）のみを受け持つ独立フォームに再設計する。

**廃止理由**：
Contact 詳細画面で confidence 確認・値修正を AJAX で行う新設計に伴い、MergeForm がフィールド値や confidence を扱う責務を持たなくなったため。MergeForm の責務はマージの判定情報のみに縮小される。

---

### 確定経緯

2026-05-10、たんたんとサポート担当クロード君（A 側）のセッションで、C-3 完了後に C-4（Execute_Merge_with_Updates）着手前の設計議論として発生。「confidence の確認をコンタクト詳細画面でやったらどうかな」というたんたんの提案を起点に、業務パターン 1 〜 6 の検証、note アペンド方式 vs 履歴表示 UI 方式の比較、UI 設計（個別フィールド + 一括確定の 2 段階方式、案 Y-2 色付き枠デザイン）の議論を経て確定。

### 担当ブランチ

B 側（D-3 ブロック実装ブランチ予定）

### ステータス

廃止確定（仕様書本体への反映は D-3 完了後）。新設計に伴う新規追加項目（Contact 詳細画面の AJAX 仕様、`contact.update_field()` モデルメソッド、AJAX 3 エンドポイント、マージ画面の新構造詳細）は D-3 実装で確定後に別途ストック追加予定。

---

## #21 Person.mark_as_active() メソッドの新規追加（mark_as_merged との対称ペア命名確定）

**対象仕様書**：統合最終版

### 項目名
Person のマージ復元用インスタンスメソッド `mark_as_active()` を新規追加し、`mark_as_merged()` との対称ペアとして命名・配置する

### 現状仕様（仕様書の該当章節）

**第10章 §10.4.1 Person のインスタンスメソッド**

現状、`Person.mark_as_merged(surviving_person)` は記載されているが、その逆操作（merged → active への状態復元）に対応するインスタンスメソッドは仕様書に明示されていない。§9.5.2 復元処理フローのステップ5「`merged_person.status` を `'active'` に戻す、`merged_into` を NULL に」が直書きで記述されている。

### 改訂内容

**(1) §10.4.1 Person のインスタンスメソッド表に追加**

| メソッド | 責務 | 配置先 |
|---|---|---|
| `person.mark_as_active()` | Person を active 状態に戻す（復元処理時に呼ばれる）。`status='active'`、`merged_into=NULL` をセット。primary_contact の復元はステップ6 で別途 `Person.set_primary_contact()` が同期するため、本メソッドには含めない。archived → active も汎用化（archived 中は対象パーソンを誰も触れないため安全） | persons/models.py |

**(2) `mark_as_merged()` ↔ `mark_as_active()` を対称ペアとして §10.4.1 内で隣接配置**

メソッド命名思想として「`mark_as_*` シリーズで状態遷移を表現する」を §10.4.1 冒頭に明記する（PersonMergeLog の `mark_as_undone()` 等とも命名スタイルが揃う設計趣旨）。

**(3) §9.5.2 復元処理フロー ステップ5の書き換え**

現状の直書き記述「`merged_person.status` を `'active'` に戻す、`merged_into` を NULL に」を、`merged_person.mark_as_active()` を呼ぶ形に書き換える。

### 改訂理由

C-3（Execute_Merge_Undo）実装にあたって、復元処理ステップ5を直書きするか、Person のインスタンスメソッドに切り出すかが論点となった。当初は `unmark_as_merged()` という命名が議論されたが、たんたんと A 側サポートクロード君の協議により、以下の理由で `mark_as_active()` への変更が確定した：

1. **対称性**：`mark_as_merged()` の逆操作は「mergedにする」の逆＝「activeにする」であり、`mark_as_active()` の方が自然
2. **読みやすさ**：「active にする」が関数名から直接読める（`unmark_as_merged()` の「unmark」がやや不自然）
3. **命名スタイル統一**：`mark_as_*` シリーズで揃える（PersonMergeLog.mark_as_undone() 等とも整合）
4. **archived → active も汎用化できる**：「active なパーソンにしかこれは呼ばれない」という業務制約はあるが、archived 中は対象パーソンを誰も触れないため、汎用メソッドとして実装しても安全（たんたん判断）

### 影響範囲

- §10.4.1 Person のインスタンスメソッド（mark_as_active 追加、mark_as_merged との対称配置）
- §9.5.2 復元処理フロー ステップ5（直書き → mark_as_active() 呼び出しに書き換え）
- 別表 A.5 Person のフィールド対照表（メソッドは対象外、変更不要）
- 第13章 Execute_Merge_Undo サービスのフロー記述（mark_as_active 呼び出しに変更）

### 確定経緯

2026-05-09 たんたんと A 側サポートクロード君のセッションで命名議論。

| 検討された候補 | 採用判断 |
|---|---|
| `unmark_as_merged()` | 不採用（unmark がやや不自然） |
| `restore_as_active()` | 不採用（restore は復元という業務概念で、状態遷移の対称性が弱い） |
| `revert_merge()` / `unmerge()` | 不採用（mark_as_* シリーズと命名スタイルがズレる） |
| **`mark_as_active()`** | **採用（mark_as_merged との対称性・命名統一）** |

実装は X-6 として完了。

### 担当ブランチ

A 側（feature/v1.4.2-models）

### ステータス

実装済み（コミット cef83c4 で push 済み、persons/models.py に +33 行）。仕様書側の記述追加のみ残っている。

---

## #22 §12.8.3 / §9.3.1 / §9.5.2 のマージ復元処理フロー記述修正

**対象仕様書**：統合最終版

### 項目名
マージ復元処理（recover）に関する仕様書記述の修正：§12.8.3 の責務記述を呼び出し元側に整理、§9.3.1 の手順順序を明示、§9.5.2 復元処理フローに手順 8（duplicate_checked_at リセット）を追加

### 現状仕様（仕様書の該当章節）

**第12章 §12.8.3 recover 処理の手順詳細**

> 手順 2：当該マージの DuplicateCandidate を 'merged' に変更
> 手順 4：duplicate_checked_at の更新を recover 内で実行

→ recover 関数の責務として記述されているが、C-3 実装で「recover 関数は冪等性のための防御のみ、状態変更の主体は呼び出し元（Execute_Merge_*）」という責務分担が確定したため、記述が実装と乖離している。

**第9章 §9.3.1 マージ実行の手順**

`merged_person.mark_as_merged(surviving_person)` と `candidate.mark_as_merged(user, review_result, note)` の呼び出し順序が明示されていない。recover の前にこの2つを呼ぶ必要があることが本文から読み取れない。

**第9章 §9.5.2 復元処理フロー**

現状の手順1〜7に、`merged_person.primary_contact.duplicate_checked_at = None` をセットする手順が含まれていない。復元後に該当 Contact が次回 cron で再判定対象に入るためには、duplicate_checked_at をリセットする必要がある。

### 改訂内容

**(1) §12.8.3 手順 2 の書き換え**

「当該マージの DuplicateCandidate を 'merged' に変更」→ 「呼び出し元（Execute_Merge_*）が事前に candidate.mark_as_merged() を呼ぶ前提。recover 関数内では冪等性のための防御チェックのみを行う」

**(2) §12.8.3 手順 4 の書き換え**

「duplicate_checked_at の更新を recover 内で実行」→ 「duplicate_checked_at の更新は呼び出し元（Execute_Merge_*）の責務。recover 関数では行わない」

**(3) §9.3.1 手順順序の明示**

マージ実行のフローに以下を明記：

> マージ実行サービス（Execute_Merge_Only / Execute_Merge_Undo）は、recover 関数を呼ぶ前に以下を順に実行する：
> 1. `merged_person.mark_as_merged(surviving_person)`
> 2. `candidate.mark_as_merged(user, review_result, note)`
> 3. recover 関数（冪等性のための防御チェックのみ）

**(4) §9.5.2 復元処理フローへの手順 8 追加**

現状の手順1〜7の後に、以下の手順 8 を追加：

> 8. `merged_person.primary_contact.duplicate_checked_at = None` をセット（次回 cron で再判定対象にするため）

### 改訂理由

C-3（Execute_Merge_Undo）実装の論点整理で、以下が確定したため：

1. **recover 関数の責務縮小**：recover は「冪等性のための防御チェック」のみを担い、状態変更の主体は呼び出し元（Execute_Merge_*）に集約する。これは「DB 履歴を見る判断」を generate 側に集約する設計（#19 X-3 関連）と同じ思想。
2. **手順順序の明示**：mark_as_merged の呼び出し順序が暗黙のままだと、コード君の実装でブレが生じる可能性が高い。仕様書側で明示する。
3. **duplicate_checked_at リセット**：復元後に Contact が再判定対象に入るには duplicate_checked_at = None が必要。仕様書に明示しないと実装漏れの可能性。

### 影響範囲

- §12.8.3 recover 処理の手順詳細（手順 2 / 手順 4 の書き換え）
- §9.3.1 マージ実行の手順（呼び出し順序の明示）
- §9.5.2 復元処理フロー（手順 8 の追加）
- §13.4.1 サービス層の Execute_Merge_* シグネチャ・責務記述（必要に応じて整合確認）
- 第10章 §10.4.1 / §10.7.1 の関連メソッド記述（必要に応じて整合確認）

### 確定経緯

2026-05-09〜2026-05-10 たんたんと A 側サポートクロード君のセッションで C-3 実装に向けた論点整理。クロード君が「DB 表層を見て業務仕様を見落とす癖」に陥らないよう、たんたんから「過去議論を見て」と指摘を受け、業務仕様（recover の責務、復元後の再判定の必要性）に基づいて確定。

C-3 実装はコミット b03787b で完了。仕様書側の修正は未着手。

### 担当ブランチ

A 側（feature/v1.4.2-models）

### ステータス

方針確定（仕様書側の記述修正のみ残っている）

---

## #23 Person.transfer_contacts_to() の挙動明文化（Case A〜D ステータス遷移との対応）

**対象仕様書**：統合最終版

### 項目名
Person.transfer_contacts_to(surviving_person, merge_reason) の挙動を、§9.4 マージ前後のステータス遷移表（Case A〜D）と対応付けて明文化する

### 現状仕様（仕様書の該当章節）

**第10章 §10.4.1 Person のインスタンスメソッド**

現状の記述：

| メソッド | 責務 | 配置先 |
|---|---|---|
| `person.transfer_contacts_to(surviving_person, merge_reason)` | 自身のコンタクト群を surviving に引き渡す（merge_reason に応じて 9.4 状態遷移を適用、全 Contact 対象：primary / active / inactive すべて） | persons/models.py |

→ 概要の記述はあるが、具体的に Case A〜D（§9.4 のステータス遷移表）のどのケースでどのフィールドがどう変わるかが §10.4.1 と §9.4 で分離して読まないと分からない。実装（X-4 / コミット 62a8ea9）と対応付けて明文化する必要がある。

**第9章 §9.4 マージ前後のステータス遷移**

PDF 別添「マージ前後のコンタクトのステータス等まとめ.pdf」と本文の Case A〜D 記述があるが、`transfer_contacts_to` メソッドとの対応関係が明示されていない。

### 改訂内容

**(1) §10.4.1 transfer_contacts_to の責務記述を拡張**

現状の1行記述を、以下の構造化された記述に拡張：

| 観点 | 記述内容 |
|---|---|
| 引数 | surviving_person（マージ先 Person）、merge_reason（DuplicateMergeReason 7値） |
| 対象 | 自 Person に紐づく全 Contact（status=primary / active / inactive すべて） |
| 処理 | merge_reason に応じて Case A〜D（§9.4）のステータス遷移を適用 |
| Case A | same_card 等：直接更新パターン（§9.4.1） |
| Case B | transfer / promotion 等：標準的な引き渡しパターン（§9.4.2） |
| Case C | additional_role：別肩書追加の特殊パターン（§9.4.3、partial unique constraint との関係に注意） |
| Case D | 復元時：previous_* を NULL にする不変原則を保つ（§9.4.4） |
| 制約 | partial unique constraint（Person.primary_contact が高々1件）に違反しないよう、引き渡し順序とステータス遷移を内部的に制御する |

**(2) §9.4 PDF 別添の参照を §10.4.1 から明示的にリンク**

§10.4.1 の transfer_contacts_to の記述に「詳細は §9.4 および別添 PDF『マージ前後のコンタクトのステータス等まとめ.pdf』を参照」と明記する。

**(3) サバイブ側 previous_* 不変原則の明文化**

§10.4.1 transfer_contacts_to の責務記述、または §9.4 内で以下を明記：

> **サバイブ側 previous_* 不変原則**：transfer_contacts_to はマージド側 Person の Contact を引き渡す処理であり、サバイブ側 Person の Contact の previous_* フィールドには一切触れない（業務所有権の分離）。

**(4) additional_role の特殊挙動の明文化**

Case C（additional_role）では、引き渡し前に partial unique constraint の制約を考慮した順序制御が必要。具体的には：

- マージド側の primary Contact を一時的に active に降格してから引き渡す
- サバイブ側の primary は維持
- 引き渡し後に必要に応じて Person.set_primary_contact() を呼ぶ

この挙動を §10.4.1 または §9.4.3 で明文化する。

### 改訂理由

X-4（Person.transfer_contacts_to() の実装）が完了し（コミット 62a8ea9）、Case A〜D の検証も全て pass、PDF 表と DB 状態一致を確認済み。実装が確定したため、仕様書側でも実装と整合する形で挙動を明文化する。

特に以下の点が、仕様書の現状記述では読み取りにくい：

1. **partial unique constraint との関係**：Case C（additional_role）で順序制御が必要なこと
2. **サバイブ側 previous_* 不変原則**：マージド側の処理がサバイブ側に波及しないこと
3. **全 Contact 対象**：primary / active / inactive すべてが対象（一部のみではない）

仕様書本文で明文化しないと、後続実装（C-1+2 / C-3 / C-4）や、将来の保守時に誤解の余地が残る。

### 影響範囲

- §10.4.1 Person のインスタンスメソッド（transfer_contacts_to の責務記述拡張）
- §9.4 マージ前後のステータス遷移（transfer_contacts_to との対応明示）
- §9.4.3 Case C additional_role（partial unique constraint との関係明文化）
- 別添 PDF「マージ前後のコンタクトのステータス等まとめ.pdf」との対応関係
- 別表 A.5 Person のフィールド対照表（メソッド対象外、変更不要）

### 確定経緯

2026-05-09 X-4 実装完了（コミット 62a8ea9、persons/models.py に +89 行）。検証スクリプトで Case A〜D 全て pass、PDF 表と DB 状態一致、既存テスト影響なし、python manage.py check も通過済み。

たんたんと A 側サポートクロード君で実装内容のレビューを通じて、仕様書側で明文化すべき項目を特定。

### 担当ブランチ

A 側（feature/v1.4.2-models）

### ステータス

実装済み（コミット 62a8ea9 で push 済み）。仕様書側の記述追加のみ残っている。

---

## #24 PersonDetailView の役割変更（active は ContactDetailView へリダイレクト、merged / archived は専用詳細画面）

**対象仕様書**：統合最終版

### 項目名
PersonDetailView の役割を Person.status 別に二系統化する。active な Person は当該 Person の primary_contact へリダイレクト、merged / archived な Person は専用詳細画面として残す。

### 現状仕様（仕様書の該当章節）

**第11章 §11.3 URL 一覧表**：8 番 PersonDetailView は「Person 中心の独立画面」として定義されている。

**第11章 §11.4 ビュー層の設計方針**：PersonDetailView は active な Person の primary_contact 情報、active コンタクト一覧、マージログ等を表示する独立画面として記述されている。

**第11章 §11.2 ディレクトリ構成**：persons/views.py に PersonDetailView を配置。

### 改訂内容

PersonDetailView の役割を Person.status 別に以下に変更する。

| Person.status | PersonDetailView の挙動 |
|---|---|
| active | 当該 Person の primary_contact を取得し、`/contacts/<primary_contact_uuid>/` へ HTTP 302 リダイレクト |
| merged | merged 状態の Person 詳細画面を表示（過去どんな Contact があったか、誰と統合されたか、過去のマージログ等） |
| archived | merged と同様、archived 状態の Person 詳細画面を表示（復元ボタン等の操作起点として将来活用） |

active な Person を Person 中心の独立画面で見ることはなくなる。primary_contact を持つ active な Person はすべて Contact 詳細画面（11 番、ContactDetailView）に集約される。

### 改訂理由

業務目線で「Person を中心とした操作」と「Contact 単位の値修正・確認」を統合的に行いたいというたんたん判断。

データ的に Contact ↔ Person は active 期間中 1:1 の関係であり、Person 詳細画面と Contact 詳細画面が別画面だと、ユーザーが「同じ人物の情報を別画面で行ったり来たり」することになる。Contact 詳細画面に集約することで業務フローがすっきりする。

merged / archived な Person は primary_contact が存在しない（または無効）ため、独立画面が必要。「過去どんな人物だったか」「誰と統合されたか」を表示する責務を持つ。archived な Person は将来「archived 一覧画面」（§10.4.2 の `Person.get_archived()` 経由）から復元する際の操作起点として活用する想定。

### 影響範囲

- §11.3 URL 一覧表（8 番 PersonDetailView の役割記述）
- §11.4 ビュー層の設計方針（PersonDetailView の節）
- §11.2 ディレクトリ構成（特に変更なし、配置場所は persons/views.py のまま）
- 第10章 §10.4 Person のモデルメソッド（必要に応じて get_primary_contact 等のメソッド追加）
- 別表 A.6 URL 一覧表（PersonDetailView の役割記述）

### 確定経緯

2026-05-10 D-3b 実装議論中にたんたん判断で確定。経緯：

- 当初は ContactDetailView と PersonDetailView を独立画面として実装する方針だった
- たんたんから「Person / Contact / 名刺詳細を 1 画面に集約する」案が出た
- 議論の結果「Person 中心の操作（マージ起点等）が見えにくくなる」懸念が出て、Person 主軸案に再考
- 最終的に「active な Person は ContactDetailView へリダイレクト、merged / archived は専用画面」で確定

### 担当ブランチ

A 側（feature/v1.4.2-models）

### ステータス

方針確定（PersonDetailView の改修自体は後続タスクで実装、ContactDetailView の D-3b は #25 として実装済み）

---

## #25 ContactDetailView の業務メイン画面化と新規セクション追加（他のアクティブコンタクト / マージ関連）

**対象仕様書**：統合最終版

### 項目名
ContactDetailView の責務を「業務メイン画面」として拡張する。Contact.status / Person.status による表示モード分岐、編集可能モードでの値修正・confidence 確認 UI、新規セクション 2 つ（他のアクティブコンタクト / マージ関連）を追加する。

### 現状仕様（仕様書の該当章節）

**第11章 §11.3 URL 一覧表**：11 番 ContactDetailView は「Contact 詳細表示画面」として最小限の定義。

**第11章 §11.4 ビュー層の設計方針**：ContactDetailView は GET 専用、Contact 単体の詳細表示が責務として記述されている。

### 改訂内容

**(1) 業務メイン画面としての位置づけ**

| Contact.status | Person.status | 表示モード | 用途 |
|---|---|---|---|
| primary | active | 編集可能 | AJAX で値修正・confidence 確認可、業務メイン画面 |
| active | active | 編集可能 | 同上、別肩書追加された副コンタクト用 |
| inactive | active | 表示のみ | 履歴閲覧 |
| 任意 | archived / merged | 表示のみ | 履歴閲覧（操作不可） |

編集可能モード判定：`Contact.status in ('primary', 'active')` AND `Contact.person.status == 'active'` のときのみ編集可能。それ以外は表示のみモード。D-3c で実装した AJAX エンドポイント（ajax-update-field / ajax-confirm-fields）の View 層ガードと整合する。

**(2) 画面構成**

ContactDetailView は以下のセクションを持つ：

- 操作ボタンセクション：修正画面ボタン（primary → 12 番、active → 13 番、inactive → 非表示）、別肩書追加ボタン（primary のみ → 9 番）、マージ画面ボタン（マージ候補があれば → 17 番）
- Contact ヘッダーセクション：Contact 名（full_name）、status バッジ、マージ候補バッジ、名刺画像（{% card_image %} 流用、モーダル拡大）、名刺レコードへの開閉式リンク（CardDetailView へ）
- 一括確定ボタンセクション（編集可能モードのみ）：{% contact_confidence %} 集計サマリー、一括確定ボタン（3 段階文言）
- フィールド表示セクション：high はシンプル表示、mid / low は {% confidence %} + ラジオボタン UI（編集可能モードのみ）
- **他のアクティブコンタクトセクション（新規）**：同じ Person 配下の他の active Contact 一覧（自分以外、リンクのみ）
- **マージ関連セクション（新規、開閉式）**：重複候補（DuplicateCandidate.get_pending）への 16 番リンク、マージログ（PersonMergeLog.get_for_person）への 20 番リンク、previous_person への Person 詳細画面（8 番）リンク

**(3) 新規セクションの設置理由**

- 「他のアクティブコンタクト」セクション：Person 配下に複数 active Contact があるケース（別肩書追加で生まれる）に対応する
- 「マージ関連」セクション：previous_person を含めてマージ経緯を辿る業務フローに対応する

### 改訂理由

PersonDetailView の役割変更（#24）に伴い、Contact 詳細画面が「inactive 表示 + primary / active の値修正」の業務メイン画面として位置付けられる。

「他のアクティブコンタクト」セクションは、Person 配下に複数 active コンタクトがあるケース（別肩書追加で生まれる）に対応するため必要。「マージ関連」セクションは previous_person を含めてマージ経緯を辿る業務フローに対応するため必要。

### 影響範囲

- §11.3 URL 一覧表（11 番 ContactDetailView の役割記述）
- §11.4 ビュー層の設計方針（ContactDetailView の節、編集可能モード判定）
- §11.8 UI カスタムタグ（{% confidence %} / {% contact_confidence %} の利用画面に Contact 詳細画面を追加）
- 別表 A.6 URL 一覧表（ContactDetailView の役割記述）
- §10.5 Contact のモデルメソッド（get_field_confidences の利用画面記述）

### 確定経緯

2026-05-10 D-3b 実装議論中にたんたん判断で確定。レイアウト案はたんたんが提示。実装はコミット fec32d4 で完了、テスト 60 / 60 PASS。

未実装の関連 URL（12 / 13 / 9 / 8 / 16 / 17 / 20 番）は href="#" + 「準備中」プレースホルダで設置済み。後続タスクで URL 差し替え予定。

### 担当ブランチ

A 側（feature/v1.4.2-models）

### ステータス

実装済み（コミット fec32d4 で push 済み、テスト 60 / 60 PASS）。仕様書側の記述追加・修正のみ残っている。

---

## #26 BackNavigator 使い方ガイド §9 画面遷移イメージの修正（push_current(title, []) の実装非整合解消）

**対象仕様書**：BackNavigator ガイド（**統合最終版とは別文書**）

### 項目名
BackNavigator 使い方ガイド §9 画面遷移イメージにおいて、詳細画面の例で `push_current('ブログ詳細', [])` と書かれている記述を、実装と整合する形に修正する。

### 現状仕様（該当箇所）

**BackNavigator 使い方ガイド §9 画面遷移イメージ**

詳細ページの例として、以下の記述がある：

```
② 詳細ページ（/blog/123/?back_stack=W3s...）
   └ push_current('ブログ詳細', [])  ← keys=[] と書かれている
   └ コメントリンク: {% append_url comment_url back %}
```

### 改訂内容

実装と整合させるため、以下のいずれかに修正する。具体的な書き方はオーパス君判断とするが、現実装の慣例に従うのが自然。

**案 A**：詳細画面の例から push_current 呼び出しを削除する。

```
② 詳細ページ（/blog/123/?back_stack=W3s...）
   └ （push_current は呼ばない）
   └ コメントリンク: {% append_url comment_url back %}
```

**案 B**：詳細画面でも push_current を呼ぶケースを書く（keys に何かしら必須）。

オーパス君判断による選択を想定。

### 改訂理由

BackNavigator の実装（back_navigator.py）では、`push_current(title, keys)` で `keys=[]` を渡すと DEBUG 時に `ValueError`（「BackNavigator.push_current: keys must not be empty」）を発生させる仕様。本番環境でも warning ログ出力 + return で何も処理されない動作となる。

しかし使い方ガイド §9 では「② 詳細ページで `push_current('ブログ詳細', [])`」と書かれており、実装と矛盾する。実装側の慣例（既存 cards 詳細画面では push_current を呼ばず、`BackNavigator(request)` の生成のみ）と整合させる必要がある。

### 影響範囲

- BackNavigator 使い方ガイド §9 画面遷移イメージ
- BackNavigator 使い方ガイド §8 NG パターン（「keys を空にする」NG パターン記述との整合）

### 確定経緯

2026-05-10 D-3b 実装中にコード君 A から発見。クロード君（A 側サポート担当）の指示書 §5.5 で `back.push_current(title=str(contact), keys=[])` を書いていたが、実装側で ValueError になる仕様。コード君 A が「既存 cards 詳細画面の慣例」として push_current を呼ばない判断をして実装。

### 担当ブランチ

共通（実装側は既に該当の慣例で実装されているため、ガイドの修正のみ）

### ステータス

実装側完了（既存 cards 詳細画面 / Contact 詳細画面ともに push_current を呼ばない実装で統一）。ガイドの修正のみ残っている。

---

## #27 Contact.full_name の空文字制約追加（DB 制約 + Form clean + AJAX View ガード）

**対象仕様書**：統合最終版

### 項目名
Contact.full_name を必須フィールドとして扱う制約を、DB レベル / Form clean / AJAX View ガードのすべての経路で追加する。

### 現状仕様（仕様書の該当章節）

**§4.4.1 Contact のフィールド定義**（行 251-284 のテーブル）

```
| full_name | CharField(255) | 氏名（正規化済み） |
```

blank / null や必須制約に関する記述が現状ない。

**§15.5.3 各フィールドの正規化ルール**（行 2548-2619 の「フルネーム（full_name）」項）

```
#### フルネーム（full_name）

- 全角空白 → 半角空白に統一
- 半角空白を除去（空白なしで比較）
```

空文字バリデーションへの言及なし。

### 改訂内容

**(1) §4.4.1 Contact フィールド定義テーブル**

full_name 行を以下に変更：

```
| full_name | CharField(255, blank=False) | 氏名（正規化済み、必須） |
```

加えて、テーブル直下に補足文を追加（または末尾の「必須／任意の補足」セクションに追記）：

> full_name は必須フィールド。OCR 由来・手動入力・マージ画面・AJAX 更新を含むすべての経路で空文字を弾く。

**(2) §15.5.3 フルネーム正規化ルール**

既存箇条書きに以下を追加：

- 正規化後に空文字となった場合は ValidationError を raise する（保存・更新前バリデーション）

**(3) 関連する View / Form / Model 側の改訂指示**

- `contacts/models.py` Contact モデル：`full_name = CharField(max_length=255, blank=False)` に変更、Migration 追加
- `contacts/views.py` ContactAjaxUpdateFieldView：`new_value.strip() == ""` の場合 400 + エラーメッセージ
- `ContactCreateView` / `UpdatePrimaryContactView` / `UpdateActiveContactView`：Form clean メソッドで空文字検証
- マージ画面側（D-4 以降）：実装時に同制約を組み込む

### 改訂理由

D-3d-3（個別フィールド値修正の AJAX 連携、コミット f59cd4a）の動作確認中に、フルネームを空文字に更新できてしまうことが判明。たんたんから「DB で規制がいる」と指摘あり。データ整合性の観点から、すべての経路で空文字を弾く必要がある。

### 影響範囲

- §4.4.1 Contact フィールド定義
- §15.5.3 正規化ルール（フルネーム項）
- `contacts/models.py` + Migration
- `contacts/views.py`（AJAX View）
- 12 番 UpdatePrimaryContactView / 13 番 UpdateActiveContactView / 10 番 ContactCreateView の Form clean
- マージ画面（D-4 以降の実装時）

### 確定経緯

2026-05-11、D-3d-3 動作確認中にたんたん指摘で確定。「フルネーム空文字制約は別タスクとして仕様書修正 → モデル修正の流れで対応」（案 X）。

### 担当ブランチ

A 側（feature/v1.4.2-models）

### ステータス

方針確定、仕様書改訂前、実装も未着手。仕様書改訂後にコード君 A へ実装依頼予定。

---

## #28 CardDetailView の業務画面化（Contact 編集 UI 追加、_contact_field.html パーツ再利用）

**対象仕様書**：統合最終版

### 項目名
CardDetailView を OpenCV デバッグ用途に加えて業務画面化する。Contact 編集 UI（個別フィールドの「確認 OK」「値修正」）を併設し、`_contact_field.html` パーツを ContactDetailView と共通化する。

### 現状仕様（仕様書の該当章節）

**§11.3 URL 一覧表**（行 1478-1508）

```
| 4 | `/cards/<uuid:pk>/` | GET | CardDetailView | 名刺詳細 |
```

役割は「名刺詳細」のみで、Contact 編集機能の記述なし。

**§11.4 View 層の設計**（行 1513-1577、または該当章節）

CardDetailView の責務記述は OCR デバッグ用途中心と推察（該当節はオーパス君判断で確認）。

**§11.8 UI カスタムタグ**（行 1900-1962）

`{% confidence %}` / `{% contact_confidence %}` の利用画面の記述に CardDetailView が含まれていない可能性。

### 改訂内容

**(1) §11.3 URL 一覧表（No. 4 CardDetailView の行）**

役割記述を変更：

```
| 4 | `/cards/<uuid:pk>/` | GET | CardDetailView | 名刺詳細 + Contact 編集（OpenCV デバッグセクションと業務操作セクションを併設）|
```

**(2) §11.4 View 層の設計**

CardDetailView 節（既存または新設、該当節はオーパス君判断）に以下を追記：

- CardDetailView は OpenCV デバッグ用途に加え、Contact 編集 UI（個別フィールドの「確認 OK」「値修正」）を提供する業務画面でもある
- 編集可能モード判定式は ContactDetailView と同一：Contact.status ∈ {primary, active} AND Person.status == 'active'
- 表示するフィールドのセットは ContactDetailView と完全一致（last_name / first_name / postal_code / lang を含む）
- テンプレートは `templates/contacts/_contact_field.html` を include して ContactDetailView と共通化

**(3) §11.8 UI カスタムタグ**

`{% confidence %}` / `{% contact_confidence %}` の利用画面リストに CardDetailView を追加。

**(4) §13 章または該当章（cards ビュー層の責務）**

該当箇所（オーパス君判断）に CardDetailView の業務画面化責務を追記。

**(5) 3 つの詳細ビューの業務的位置づけ（新規追記）**

仕様書のどこか（§11.4 か新規節、オーパス君判断）に以下を明記：

- PersonDetailView：起点（業務の入口、Person 単位の操作主軸）
- ContactDetailView：業務メイン画面 + inactive 履歴閲覧
- CardDetailView：OpenCV デバッグ + Contact 編集の両方を担う

### 改訂理由

D-3 ブロック完了後、CardDetailView の業務的位置づけを確定する必要があった。Person 起点の業務フローでも、名刺画像を見ながら confidence 修正・確認をしたい場面があり、ContactDetailView と機能を完全に切り分けるのではなく併存させる方が業務効率が高い、というたんたん判断。

3 画面（PersonDetailView / ContactDetailView / CardDetailView）はすべて残し、業務目的を分けて運用する。

### 影響範囲

- §11.3 URL 一覧表（No. 4 CardDetailView の役割記述）
- §11.4 View 層の設計
- §11.8 UI カスタムタグ
- §13 章 または cards ビュー層の責務記述（該当節はオーパス君判断）
- 別表 A.6 URL 一覧表（PDF 側、参照箇所のみ）
- `cards/views.py` / `templates/cards/card_detail.html` / `cards/tests.py`

### 確定経緯

2026-05-11、たんたん判断で確定。実装はコミット c1c7686 で完了、cards 15/15 + contacts 65/65 PASS。

過去議論の経緯：たんたんから「confidence の修正・確認は Card / Person 両方で必要」「アプリ的には起点が Person、非アクティブな Contact は ContactDetailView、Card は OpenCV デバッグも見たい」との明示指示あり。

### 担当ブランチ

A 側（feature/v1.4.2-models）

### ステータス

実装済み（コミット c1c7686 で push 済み）。仕様書側の記述追加のみ残っている。

---

## #29 ContactListView 新規追加（§11.3 / §20.1 改訂、検索フォーム付き一覧画面）

**対象仕様書**：統合最終版

### 項目名
Contact 一覧画面（ContactListView）を新規追加する。`/contacts/` ルートに 7 フィールド検索フォーム + status 3 チェックボックス絞り込み + ページネーション + BackNavigator 連携を備えた一覧画面を実装する。あわせて §20.1「v1.4.2 で実装しないもの」リストから Contact 一覧画面の項目を削除する。

### 現状仕様（仕様書の該当章節）

**§11.3 URL 一覧表**（行 1505）

```
一覧画面なし：`/contacts/`（Contact 一覧）、`/persons/<uuid>/update/`（Person 編集）。
```

**§20.1 v1.4.2 で実装しないもの**（行 2762）

```
- Contact 一覧画面は実装しない（Person 一覧と名刺一覧で代替）
```

### 改訂内容

**(1) §11.3 URL 一覧表**

行 1505 の「一覧画面なし」の記述から `/contacts/`（Contact 一覧）を削除：

```
一覧画面なし：`/persons/<uuid>/update/`（Person 編集）。
```

URL 一覧テーブルに新規行を追加（No. 番号は採番ルールに従う。既存番号の空き枠 No. 18 を使うか、末尾に追加するかはオーパス君判断）：

```
| (新No) | `/contacts/` | GET | ContactListView | コンタクト一覧画面。7 フィールド検索（氏名 / 会社 / 部署 / 役職 / メール / 電話 / 住所）、status 3 チェックボックス絞り込み（primary / active / inactive、デフォルト primary のみ）、ページネーション 20 件/ページ、BackNavigator 連携 |
```

**(2) §20.1 v1.4.2 で実装しないもの**

行 2762 の以下を **削除**：

```
- Contact 一覧画面は実装しない（Person 一覧と名刺一覧で代替）
```

**(3) §11.4 View 層の設計**

ContactListView 節を新設。以下を明記：

- URL：`/contacts/`
- Model：Contact
- paginate_by：20
- 検索フィールド：7 フィールド AND 検索（氏名 / 会社 / 部署 / 役職 / メール / 電話 / 住所）
- 電話フィールドは phone / mobile / fax の OR 一致
- status 絞り込み：primary / active / inactive の独立チェックボックス、初回アクセス時のデフォルトは primary のみ ON、検索ボタン押下後は選択された status のみ表示（全外しで 0 件）
- Person.status='active' のみ表示（merged Person 配下は常に除外）
- 並び順：updated_at 降順、created_at 降順を二次キー
- BackNavigator 連携：詳細画面から戻った時に検索条件 / status 選択 / ページ番号を保持

**(4) サイドバー（仕様書記述の場所があれば）**

`templates/cards/_sidebar.html` の「名刺管理」セクションに「コンタクト一覧」リンクを追加。仕様書にサイドバーメニュー構成の節があればそこに反映（該当節はオーパス君判断）。

**(5) §11.5 認証方針 または該当節**

ContactListView は ContactDetailView と同様、Contact が user FK を持たないため `get_current_user()` は呼ばない。**仕様書 §18.2 の方針との整合は仕様書改訂時に再確認する**（コード君 A の指摘により、サポート担当 + たんたんで承認済み。実装は コミット 53ff061 push 済み）。

### 改訂理由

2026-05-11 本セッションで、たんたんから「Contact 一覧画面がないので作りたい」との明示指示。サポート担当クロード君が仕様書との食い違いを論点として提示したが、業務的に Contact 単位の一覧が必要との判断で「新規追加」確定。

Person 一覧 + 名刺一覧では Contact 単位の絞り込み（例：副コンタクトの横断検索）がしにくい、という業務上の課題が背景。

### 影響範囲

- §11.3 URL 一覧表（一覧画面なしリスト + テーブルに新規行）
- §11.4 View 層の設計（ContactListView 節を新設）
- §20.1 v1.4.2 で実装しないもの（Contact 一覧画面の行を削除）
- §18.2 認証方針との文言整合（仕様書改訂時に再確認）
- 別表 A.6 URL 一覧表（PDF 側、参照箇所のみ）
- `contacts/views.py` / `contacts/urls.py` / `templates/contacts/contact_list.html`（新規） / `contacts/tests.py` / `templates/cards/_sidebar.html`

### 確定経緯

2026-05-11、本セッションで論点提示 → たんたん判断で「Contact 一覧画面を新規追加」確定。コミット 53ff061 で実装完了。

実装に至るまでの軽微な仕様変更：

- 初版（status 絞り込み = 単一チェック「inactive を含める」）→ たんたんのブラウザ動作確認後、3 独立チェックボックス（primary / active / inactive）に変更
- サイドバーへのリンク追加も同コミットに含めた

加えて、コード君 A から実装着手前に方針確認があり、認証パターン（ContactListView は ContactDetailView と同じく `get_current_user()` 不要）について修正した経緯あり。

### 担当ブランチ

A 側（feature/v1.4.2-models）

### ステータス

実装済み（コミット 53ff061 で push 済み）。仕様書側の記述追加・削除のみ残っている。

---

## #30 ContactDetailView に inactive Contact 履歴セクション追加（マージ関連セクション内、共通 include 化）

**対象仕様書**：統合最終版

### 項目名
ContactDetailView のマージ関連セクション（開閉式）内に、同一 Person 配下の inactive Contact 履歴セクションを追加する。テンプレート `templates/contacts/_inactive_contacts.html` を共通 include として配置し、contacts / persons 両方から再利用する（元は `persons/templates/_inactive_contacts.html` にあったものを共通化のため移動）。

### 現状仕様（仕様書の該当章節）

§11.4 ContactDetailView の責務記述に、inactive Contact 履歴の表示は未記載。マージ関連セクションは「マージ前情報（previous_person）」「他のアクティブコンタクト」を含むが、同一 Person の inactive Contact 履歴は含まれていない。

### 改訂内容

**(1) §11.4 ContactDetailView 節**

ContactDetailView のマージ関連セクション（開閉式）の構成要素に以下を追記：

- 同一 Person 配下の inactive Contact 履歴セクション（自分自身は除外）
- デフォルト閉じた状態（マージ関連セクションの慣例維持）
- context 名は `inactive_contacts`、`contact.person.get_inactive_contacts().exclude(pk=contact.pk)` で取得

**(2) §11.8 共通 include の節（該当節があれば。なければ §11.4 内の備考でも可）**

`templates/contacts/_inactive_contacts.html` を共通 include として位置づける旨を記載：

- contacts / persons 両方から include される共通部品
- 引数 context：`inactive_contacts`（Contact の queryset、自分自身を除外済み）
- person_detail_merged.html / person_detail_archived.html / contact_detail.html の 3 箇所から include

### 改訂理由

引継ぎ資料 v5 §4.2「inactive Contact 履歴表示 UI」の対応。同一 Person 配下の inactive Contact を ContactDetailView で履歴閲覧できるようにするのが業務目的。Person 詳細画面（merged / archived）と同じ部品を再利用することで DRY を維持。

### 影響範囲

- §11.4 ContactDetailView 節（マージ関連セクション構成要素の記述）
- §11.8 共通 include の節（あれば。`_inactive_contacts.html` の位置づけ追加）
- `contacts/views.py`（ContactDetailView の context に `inactive_contacts` 追加。実装済み）
- `templates/contacts/contact_detail.html`（マージ関連セクション内に include 追加。実装済み）
- `templates/contacts/_inactive_contacts.html`（共通 include。実装済み、persons から移動）
- `templates/persons/person_detail_merged.html` / `templates/persons/person_detail_archived.html`（include パス更新済み）
- `contacts/tests.py`（inactive_contacts 関連テスト 5 件、合計 80 件）

### 確定経緯

2026-05-11、本セッションでサポート担当クロード君が論点提示。論点は (1) レイアウト = 独立セクション or マージ関連内に含める、(2) テンプレート = 共通 include 化、(3) 開閉状態 = デフォルト閉。たんたん判断で「案 B（マージ関連内）+ 案 C（共通 include 化）+ 案 A（デフォルト閉）」確定。コミット 61cfd91 で実装完了。

### 担当ブランチ

A 側（feature/v1.4.2-models）

### ステータス

実装済み（コミット 61cfd91 で push 済み）。仕様書側の記述追加のみ残っている。

---

## #31 ContactDetailView に「現在所属 Person への遷移リンク」追加

**対象仕様書**：統合最終版

### 項目名
ContactDetailView のヘッダーまたは操作ボタンセクションに、Contact.person（現在所属している Person）への遷移リンクを追加する。previous_person（マージ前の人物）への遷移リンクとは別の項目。

### 現状仕様（仕様書の該当章節）

§11.4 ContactDetailView に、現在所属 Person への遷移リンクの記載なし。テンプレート `templates/contacts/contact_detail.html` 内には previous_person 用のプレースホルダ（L312-314 周辺、マージ関連セクション内）のみ存在し、Contact.person（現在所属 Person）への遷移リンクはそもそも存在しない。

### 改訂内容

**(1) §11.4 ContactDetailView 節**

責務記述に以下を追記：

- 現在所属 Person への遷移リンク提供（URL：`/persons/<contact.person.pk>/`）
- 配置位置：操作ボタンセクション先頭、`is_editable` 条件の外に独立配置（orphan Contact では `{% if contact.person %}` ガードで非表示）
- ボタンスタイル：`app-btn app-btn--primary`
- ボタンテキスト：「人物詳細」
- BackNavigator 連携：`{% append_back_url %}` で戻り先（Contact 詳細）を引き継ぐ

### 改訂理由

2026-05-11 本セッションで PersonDetailView 実装中に発見。contact_detail.html L312-314 のプレースホルダは previous_person 用（マージ前の人物への遷移リンク）であり、「現在所属している Person への遷移リンク」がそもそも存在しないことが判明した。業務的に「この Contact が誰の名刺なのか、人物詳細を見たい」フローを実現するためにリンクが必要。

PersonDetailView の active 分岐は ContactDetailView へリダイレクトする設計（#24 参照）のため、ContactDetailView 側に Person への明示的な遷移リンクがないと、現在の所属 Person へ戻る導線が断たれる。

### 影響範囲

- §11.4 ContactDetailView 節（責務記述に追記）
- `templates/contacts/contact_detail.html`（操作ボタンセクション先頭に新規リンク追加。実装済み）
- `contacts/views.py`（context は既存の `contact.person` 経由でアクセス可能、変更不要）

### 確定経緯

2026-05-11 本セッションで気付き、たんたんとの議論で「次セッションの軽微タスクとして整理」と確定。後続セッションで配置位置を「操作ボタンセクション先頭、`is_editable` 条件の外に独立配置」と確定し、コミット fdb3805 で実装完了。

### 担当ブランチ

A 側（feature/v1.4.2-models）

### ステータス

実装済み（コミット fdb3805 で push 済み）。仕様書側の記述追加のみ残っている。

---

# OCR / OpenCV パイプライン分離関連項目（#32〜#44）

以下は B 側ブランチ（feature/v1.5.0-pipeline-split、ブランチ名は履歴的事実）における OCR / OpenCV パイプライン分離に伴う仕様改訂項目である。本セクション以降の項目は、B 側ブランチが main にマージされる際にまとめて仕様書反映する。A 側（feature/v1.4.2-models）が main にマージされた後、B 側マージ時に衝突調整（マイグレーション番号等）を実施する前提。本ストックの項目は #1〜#44 すべて v1.4.2 リリースの範囲として扱う。

ブランチ起点：0e26a4c、最終コミット：2c35db0、全 12 コミット。

---

## #32 v1.2.0 パターンA 撤回（OCR 単位を 1 OriginalImage → 1 BusinessCard に変更）

**対象仕様書**：統合最終版

### 項目名
v1.2.0 主要方針「API 1 回呼び出しで全名刺一括取得（パターンA）」の撤回。OCR 処理単位を OriginalImage 単位から BusinessCard 単位に変更。

### 現状仕様（仕様書の該当章節）

v1.2.0 主要方針および §8.5「処理パイプライン」で、1 OriginalImage = Claude API 1 リクエスト = 複数名刺一括 OCR と規定。「名刺ごとに API を叩き直す設計（パターン B）は採用しない」と明記。

### 改訂内容

- 「パターンA」の項目を **v1.4.2 で公式に撤回**する旨を明記
- v1.4.2 以降：**BC 1 枚 = Claude API 1〜2 リクエスト**
- 1 枚目は通常 OCR、orientation 補正が必要なら 2 枚目で再 OCR（→ #37）
- 1 OriginalImage 全体の OCR 完了は、その OriginalImage に紐づく全 BC の OCR 完了で判定

### 改訂理由

- OCR と OpenCV を独立した cron 2 本に分離する（→ #38 / #39）ことで、片方の障害が他方に波及しない構造に
- BC 単位処理化により、orientation 補正後の再 OCR・stuck からの BC 単位リトライ・チューニングがやりやすくなる
- 5/8 にたんたんと議論し方針確定、5/11 に実装

### 影響範囲

- v1.2.0 主要方針章
- §8.5「処理パイプライン」全体
- #33（BC モデル拡張）／ #36（関数構造）／ #38 / #39（cron 分離）／ #37（2 回 OCR）に派生

### 確定経緯

- 2026-05-08：たんたんと B 側サポートクロード君で議論。当初 v1.5.0 据え置きで合意
- 2026-05-11：たんたんから「v1.5.0 据え置きを覆して進める」確定、本セッションで実装

### 担当ブランチ

B 側（feature/v1.5.0-pipeline-split）

### マージタイミング

v1.4.2 リリース時。v1.2.0 主要方針の該当箇所には「v1.4.2 で改訂」の注記を入れる方針。

### ステータス

実装済み（feature/v1.5.0-pipeline-split 全 12 コミット、HEAD 2c35db0）。仕様書側の改訂のみ残る。

---

## #33 BusinessCard モデル拡張（raw_json_1 / raw_json_2 / ocr_status / claimed_at / error_message 追加、ocr_result null 許容化）

**対象仕様書**：統合最終版

### 項目名
v1.4.2 パイプライン分離に伴う BusinessCard モデルのフィールド追加と既存フィールド変更。

### 現状仕様（仕様書の該当章節）

- §4.3 BusinessCard モデル：v1.2.0 で「raw_json / ocr_status / error_message を持たない」と明記
- #9（ocr_result 追加）で 5 値 TextChoices、default=business_card

### 改訂内容

- **v1.2.0 ルール「BC は raw_json / ocr_status を持たない」を v1.4.2 で公式に撤回**
- BusinessCard に以下フィールドを追加：

| フィールド | 型 | null | 用途 |
|---|---|---|---|
| raw_json_1 | JSONField | 許容 | 1 回目 OCR の生 JSON 全体（OcrService.run_ocr の戻り値まるごと） |
| raw_json_2 | JSONField | 許容 | 2 回目 OCR の生 JSON 全体（orientation=normal なら null） |
| ocr_status | CharField (TextChoices) | 不可 | OCR 処理状態 |
| claimed_at | DateTimeField | 許容 | OCR cron の CAS 時刻 |
| error_message | TextField | blank 許容 | OCR 失敗時のエラー集約 |

- 新規 TextChoices `BusinessCard.OcrStatus`：`pending` / `processing` / `done` / `failed`（default=pending）
- 既存 `ocr_result` を **null 許容化**、default=None。OpenCV cron で BC 作成直後は null、OCR 完了時に既存 5 値のいずれかに確定

### 改訂理由

- OCR の進捗を BC 単位で管理する必要があるため（パターンA 撤回に伴う）
- 1 回目／2 回目両方の生 JSON を保存することで、回転補正の効果測定・プロンプトチューニングが可能
- ocr_status を独立フィールドで持つことで、ocr_result の意味を「OCR 結果の分類」のみに保てる

### 影響範囲

- §4.3 BusinessCard モデル定義
- §14.4 モデル固有の TextChoices に OcrStatus を追加
- 別表 C BusinessCard.OcrStatus 追加
- 別表 A.4 フィールド対照表に 5 フィールド追加
- マイグレーション 0010 で実装済み

### 確定経緯

- 2026-05-08 議論で「raw_json は 1 回目・2 回目とも BC に保存」「ocr_status は独立フィールド」確定
- 2026-05-11 たんたん追加確認で：raw_json_1 / raw_json_2 は OcrService.run_ocr 戻り値まるごと（cache_read_input_tokens まで含む）／ ocr_result default=None に確定

### 担当ブランチ

B 側（feature/v1.5.0-pipeline-split）

### マージタイミング

v1.4.2 リリース時。マイグレーション番号 0010 は A 側との衝突調整あり（B 側 0001-0009 + 0010-0011、A 側マイグレーションとの並行調整）。

### ステータス

実装済み（コミット 440ed4f）

---

## #34 OriginalImage.STATUS_CHOICES 拡張（opencv_processing / cards_extracted 追加）

**対象仕様書**：統合最終版

### 項目名
v1.4.2 パイプライン分離に伴う OriginalImage.status 値の追加。

### 現状仕様（仕様書の該当章節）

- §4.2 OriginalImage モデル：status は `pending` / `processing` / `extracted` / `garbage` / `failed` の 5 値
- 別表 C.1 STATUS_CHOICES に同 5 値

### 改訂内容

STATUS_CHOICES に以下 2 値を追加（**既存 5 値はすべて残置**）：

| 値 | label | 用途 |
|---|---|---|
| opencv_processing | OpenCV処理中 | OpenCV cron の CAS で claim 後、検出処理中 |
| cards_extracted | OpenCV完了・OCR待ち | OpenCV 検出完了・BC 作成済み・OCR 未実行 |

既存 `processing`（v1.4.2 改訂前の 1 本パイプライン用）は **v1.4.2 改訂後も物理残置**（後方互換）。物理削除は v1.4.x 以降の別マイグレーション。

新しい状態遷移：

```
pending
  → opencv_processing (OpenCV cron が CAS)
    → cards_extracted (OpenCV 検出成功・BC 作成)
      → (OCR cron が各 BC を処理、全 BC が done/failed になったら集計遷移)
        → extracted (成功 BC が 1 件以上あり)
        → failed (全 BC が OCR 失敗、致命的)
  → garbage (OpenCV で 0 件検出)
  → failed (OpenCV 段階の致命的失敗)
```

### 改訂理由

- パイプライン分離（→ #38 / #39）に伴い、OpenCV 完了直後の中間状態（cards_extracted）が必要
- stuck sweeper が「OpenCV 段階の滞留」「OCR 段階の滞留」を区別できるようにするため

### 影響範囲

- §4.2 OriginalImage モデル
- §14.4 モデル固有の TextChoices
- 別表 C.1 STATUS_CHOICES（5 値 → 7 値）
- views.py CardListView の 7 値フィルタ（→ #43）と整合
- stuck sweeper の対象判定（→ #38 / #39）

### 確定経緯

2026-05-11 たんたんと B 側サポートクロード君で議論、追加確認2「cards_extracted → extracted 遷移条件」確定（全 BC が done/failed のいずれかになった時点）

### 担当ブランチ

B 側（feature/v1.5.0-pipeline-split）

### マージタイミング

v1.4.2 リリース時

### ステータス

実装済み（コミット 440ed4f）

---

## #35 OriginalImage.raw_json の読み出し廃止（フィールド物理残置）

**対象仕様書**：統合最終版

### 項目名
v1.4.2 で OriginalImage.raw_json の読み出しを完全廃止し、生 JSON 参照は BC.raw_json_1 / raw_json_2 経由に統一。フィールド自体は物理残置（旧データ後方互換）。

### 現状仕様(仕様書の該当章節）

- §4.2 OriginalImage モデルに raw_json フィールド（JSONField）
- v1.2.0 主要方針で「Claude API 生レスポンスは OriginalImage.raw_json に集約保存」と明記
- §11 UI 仕様で元画像詳細・名刺詳細で OriginalImage.raw_json を参照する記述

### 改訂内容

- v1.4.2 以降、**OriginalImage.raw_json は読み出さない**。views / templates / admin から削除
- BC 1 枚ごとの生 JSON 参照は **BC.raw_json_1 / BC.raw_json_2**（→ #33）から取得
- フィールド自体は **v1.4.2 では物理残置**（既存データ救済のため）
- 物理削除は v1.4.x 以降の別マイグレーション
- 既存データはデータマイグレーション 0011（→ #44）で BC.raw_json_1 に移行済み

### 改訂理由

- パターンA 撤回（→ #32）に伴い、生 JSON の保存単位が BC に移行
- 旧 OriginalImage.raw_json を読み続けると新旧データの不整合・二重管理になる

### 影響範囲

- §4.2 OriginalImage モデル説明：「raw_json は v1.4.2 で deprecated」を注記
- §11 UI 仕様：raw_json 参照箇所をすべて BC.raw_json_1 / _2 に書き換え
- admin の readonly_fields から raw_json 削除、BC admin に raw_json_1 / _2 等を読み取り専用追加
- データマイグレーション 0011 で既存データ移行（→ #44）

### 確定経緯

2026-05-11 たんたん追加確認4 で「v1.4.2 で読み出し完全廃止、フィールド物理残置、データマイグレーション必須」確定

### 担当ブランチ

B 側（feature/v1.5.0-pipeline-split）

### マージタイミング

v1.4.2 リリース時

### ステータス

- 読み出し廃止：実装済み（コミット 0dd8e5f / 2c35db0）
- フィールド物理残置：v1.4.2 では維持、削除タイミングは未定（将来タスクストック扱い）

---

## #36 パイプライン関数構造の刷新

**対象仕様書**：統合最終版

### 項目名
v1.4.2 で OpenCV と OCR を 2 本のパイプラインに分離。新規関数群を導入。

### 現状仕様（仕様書の該当章節）

- §8.5「Extract_Cards_via_OCR」（v1.4.2 改訂前の 1 本パイプライン上位関数）
- 内部で PipelineCoordinator が OpenCV 検出 → OCR → Contact / Person 生成を直列処理

### 改訂内容

旧上位関数 `Extract_Cards_via_OCR` を撤廃し、以下の関数群に置き換える：

| 関数 | 性質 | 役割 |
|---|---|---|
| `Run_Crop_Cards_From_OriginalImage(original_image)` | 副作用あり（複合） | OpenCV パイプライン上位（cron A 経由）。検出 → BC 作成 → OriginalImage.status=cards_extracted まで |
| `Run_Process_CardImages_With_OCR()` | 副作用あり（複合） | OCR パイプライン上位（cron B 経由）。BC を CAS で claim → process_cardimage_with_ocr 呼び出し |
| `process_cardimage_with_ocr(business_card, ocr_service)` | 副作用あり（複合） | BC 1 枚を引数に、OCR 実行 → BC 更新 → Contact / Person 生成 → OriginalImage.status 集計遷移までを完結 |
| `extract_carddata_via_ocr(card_image, ocr_service)` | 副作用あり（API 呼び出し） | 1 枚の card_image に対して 1〜2 回 OCR を実行し結果を辞書で返す純粋ラッパー |
| `_rotate_card_image(image, orientation)` | 純関数 | orientation 値に応じて画像を「文字が正立になるよう」補正（Pillow Image.rotate / ImageOps.mirror） |

OcrService.run_ocr は **責務不変**（1 画像 = 1 API 呼び出しの純粋ラッパー）。

orientation → 補正回転マップ：

| orientation 値 | 補正操作 |
|---|---|
| normal | 何もしない |
| rotate_90_cw | 反時計回り 90°（image.rotate(90, expand=True)） |
| rotate_90_ccw | 時計回り 90°（image.rotate(-90, expand=True)） |
| rotate_180 | 180° 回転（image.rotate(180, expand=True)） |
| mirror | 水平反転（ImageOps.mirror(image)） |

### 改訂理由

- OpenCV と OCR の境界を関数レベルで明確に分離
- BC 1 枚単位の関数を独立させることで、Celery 化やプロンプト・パラメータチューニングがやりやすくなる（5/8 議論で確定）

### 影響範囲

- §8 パイプライン章全体
- §13 関数命名規則：上位関数の新規追加（Pascal_Snake_Case 命名）
- §8.5.4「Extract_Cards_via_OCR」廃止 → 新章へ置き換え

### 確定経緯

- 2026-05-08 たんたんから関数構造案（Run_Crop_Cards_From_OriginalImage / Run_Process_CardImages_With_OCR / process_cardimage_with_ocr / extract_carddata_via_ocr）が出され確定
- 2026-05-11 _rotate_card_image の Pillow rotate 方向を実証検証（追加検証で OCR モデルの単発判定誤りを切り分け済み、マップは正しい）

### 担当ブランチ

B 側（feature/v1.5.0-pipeline-split）

### マージタイミング

v1.4.2 リリース時

### ステータス

実装済み：

- crop_cards.py（コミット 65759d8）
- extract_carddata_via_ocr / _rotate_card_image（コミット 241cf42）
- process_cardimage_with_ocr（コミット f426cd2 / 防御チェック緩和 d25b47d）

---

## #37 条件付き 2 回 OCR の導入

**対象仕様書**：統合最終版

### 項目名
1 BC につき orientation に応じて条件付きで 2 回 OCR を実行する仕様。

### 現状仕様（仕様書の該当章節）

v1.4.2 改訂前：1 枚の card_image につき OCR は 1 回のみ。orientation は OCR レスポンスから取得して BC.orientation に保存するが、補正再 OCR は行わない。

### 改訂内容

新仕様：

1. 1 枚の card_image を `extract_carddata_via_ocr(card_image, ocr_service)` に渡す
2. 1 回目 OCR を実行 → 結果から orientation を取得
3. `orientation == 'normal'` → 1 回で完結（raw_json_2 は null）
4. `orientation != 'normal'` → `_rotate_card_image` で補正 → 2 回目 OCR
5. **1 回目／2 回目とも同じプロンプト**（フィールド抽出 + orientation 判定を同時に実施するプロンプト）
6. **2 回目失敗時は 1 回目結果を採用**（raw_json_2=None、error_message に「2 回目 OCR 失敗」を追記、ocr_status=done）
7. **1 回目自体失敗時**：ocr_status=failed、ocr_result=ocr_failed、retry_failed_ocr --ocr で差し戻し可能

採用 raw_json の選択ロジック：

- 2 回目成功時：raw_json_2 を採用
- 2 回目スキップ時／2 回目失敗時：raw_json_1 を採用

card_image の上書き保存：

- 2 回目走った時点で **必ず補正画像で BC.card_image を上書き**（2 回目失敗でも上書き、補正は実施済みのため）
- 既存 `orientation` フィールドは「**検出時の元の orientation**」を保存（補正ログとして）

mirror の扱い：

- `cv2.flip` ではなく Pillow `ImageOps.mirror` で水平反転 → 2 回目 OCR
- 他 orientation と同パイプライン、ただし精度保証なし

### 改訂理由

- 5/8 議論で「AI OCR は名刺が回転していると読み取り精度が極端に落ちる」「補正後再 OCR で精度向上」をたんたんから提案
- 同じプロンプトを使うことで 1 回目／2 回目の比較データがそのままチューニング材料になる
- 1 回目で normal なら 2 回目スキップでトークンコスト節約

### 影響範囲

- §8 OCR 仕様に新章「条件付き 2 回 OCR」を追加
- §13 関数命名：extract_carddata_via_ocr の責務記述
- プロンプトファイル（cards/prompts/extract_combined.txt）は変更なし
- §15 cards 処理ルールへの影響：採用 raw_json の選択ロジックを追記

### 確定経緯

2026-05-08 たんたんと B 側サポートクロード君で議論。条件付き 2 回方式（正立なら 1 回完結）に確定。1 回目／2 回目両方の生 JSON を保存する方針も同時確定。

### 担当ブランチ

B 側（feature/v1.5.0-pipeline-split）

### マージタイミング

v1.4.2 リリース時

### ステータス

実装済み（コミット 241cf42、追加検証で Pillow rotate 方向確認済み）

---

## #38 process_opencv 管理コマンド新設

**対象仕様書**：統合最終版

### 項目名
OpenCV パイプライン専用 cron として `process_opencv` 管理コマンドを新設。

### 現状仕様（仕様書の該当章節）

- §8.5.4「process_pending」（1 本パイプライン用、v1.4.2 で廃止 → #42）
- §17 別表 B 管理コマンド一覧

### 改訂内容

新規管理コマンド `cards/management/commands/process_opencv.py`：

| 項目 | 内容 |
|---|---|
| 対象クエリ | `status=pending` かつ **BC が 0 件** の OriginalImage |
| 並び順 | created_at 昇順 |
| 限度件数 | デフォルト 10（既存 process_pending 踏襲、`--limit` で変更可） |
| CAS | OriginalImage 単位：`status=pending` → `opencv_processing` + `claimed_at=now` の filter().update() |
| stuck sweeper | `status=opencv_processing` AND `claimed_at < now - threshold`（30 分、settings.OCR_STUCK_THRESHOLD_MINUTES）→ `pending` に戻す |
| stuck 後処理 | claimed_at=None / error_message="" / detected_count=0 / raw_json=None。BC は存在しない前提（防御的に BC 1 件以上なら警告ログ） |
| 個別指定 | `--id <uuid>` で 1 件のみ処理 |

**再実行禁止**：BC が 1 件でも存在する OriginalImage は対象外。card_index 不変担保のため。debug データ再計算は `recalc_opencv_debug` / RecalcDebugView 経由のみ。

cron 例：`*/5 * * * * python manage.py process_opencv`

### 改訂理由

- パターンA 撤回（→ #32）に伴うパイプライン分離
- OpenCV と OCR を独立 cron で動かすことで、片方の障害が他方に波及しない
- OpenCV は API 不使用なので高頻度で動かせる

### 影響範囲

- §8.5 パイプライン章：「OpenCV cron」セクション新設
- §17 別表 B：process_opencv 追加
- cron 設定の運用 docs（運用 docs があれば更新）

### 確定経緯

2026-05-11 たんたん追加確認5「OpenCV cron 再実行禁止（BC 1 件でも存在すれば対象外）」確定

### 担当ブランチ

B 側（feature/v1.5.0-pipeline-split）

### マージタイミング

v1.4.2 リリース時

### ステータス

実装済み（コミット 65759d8）

---

## #39 process_ocr 管理コマンド新設

**対象仕様書**：統合最終版

### 項目名
OCR パイプライン専用 cron として `process_ocr` 管理コマンドを新設。

### 現状仕様（仕様書の該当章節）

- §8.5.4「process_pending」（1 本パイプライン用、v1.4.2 で廃止 → #42）
- §17 別表 B 管理コマンド一覧

### 改訂内容

新規管理コマンド `cards/management/commands/process_ocr.py`：

| 項目 | 内容 |
|---|---|
| 対象クエリ | `BusinessCard.ocr_status=pending` |
| 並び順 | OriginalImage.created_at 昇順 → card_index 昇順 |
| 限度件数 | デフォルト 10（`--limit` で変更可） |
| CAS | BC 単位：`ocr_status=pending` → `processing` + `claimed_at=now` の filter().update() |
| OcrService | cron 1 起動で 1 インスタンス使い回し（プロンプト・スキーマキャッシュ共有） |
| stuck sweeper | `BC.ocr_status=processing` AND `BC.claimed_at < now - threshold`（30 分）→ `pending` に戻す |
| stuck 後処理 | BC をリセット（ocr_status=pending / claimed_at=None / raw_json_1=None / raw_json_2=None / error_message="" / ocr_result=None / orientation=normal）。card_image は不変。所属 Contact 削除、孤立 Person 削除。OriginalImage.status が extracted / failed なら cards_extracted に退行 |
| 個別指定 | `--id <bc_uuid>` で BC 1 件のみ処理 |

OriginalImage.status 集計遷移：

- 各 BC 処理完了時に、所属 OriginalImage の全 BC の ocr_status を集計
- 全 BC が `ocr_status ∈ {done, failed}` になった時点で：
  - 全 BC が failed → OriginalImage.status=failed（致命）
  - それ以外（成功 BC が 1 件以上）→ OriginalImage.status=extracted（部分失敗も extracted、追加確認2 確定）

cron 例：`*/5 * * * * python manage.py process_ocr`

### 改訂理由

- パターンA 撤回（→ #32）に伴うパイプライン分離
- OCR は API レート絡みで間隔・並列度を独自に制御できる

### 影響範囲

- §8.5 パイプライン章：「OCR cron」セクション新設
- §17 別表 B：process_ocr 追加
- cron 設定の運用 docs

### 確定経緯

2026-05-11 たんたん追加確認6「OcrService は cron 1 起動 = 1 インスタンス使い回し」、追加確認2「extracted / failed 遷移の判定」確定

### 担当ブランチ

B 側（feature/v1.5.0-pipeline-split）

### マージタイミング

v1.4.2 リリース時

### ステータス

実装済み（コミット 7c41ebf、防御チェック緩和 d25b47d）

---

## #40 retry_failed_ocr の --opencv / --ocr 分岐、ocr_recovery.py 共通ヘルパー新設

**対象仕様書**：統合最終版

### 項目名
retry_failed_ocr 管理コマンドを 2 系統に分岐し、stuck sweeper と差し戻しロジックの共通部分を新規モジュール `cards/tasks/ocr_recovery.py` に切り出す。

### 現状仕様（仕様書の該当章節）

- §17 別表 B「retry_failed_ocr」：`status=failed` AND `BC 0 件` の OriginalImage を `pending` に戻す

### 改訂内容

コマンド体系を 2 系統に分岐（mutually exclusive、どちらか必須）：

| サブコマンド | 対象 | 処理 |
|---|---|---|
| `retry_failed_ocr --opencv` | `status=failed` AND BC 0 件 の OriginalImage | `status=pending` に戻す（既存挙動相当） |
| `retry_failed_ocr --ocr` | `BC.ocr_status=failed` の BusinessCard | BC を pending に戻し、所属 OriginalImage.status を `cards_extracted` に戻す |

`--limit` / `--id` フラグ各々で個別指定可能。

共通ヘルパー `cards/tasks/ocr_recovery.py` 新設：

- `reset_business_card_to_pending(bc)`：BC の OCR 状態リセット（Contact 削除・孤立 Person 削除・BC リセット）
- `recalc_original_image_status_to_cards_extracted(oi_id)`：所属 OriginalImage.status の退行判定
- process_ocr.py の stuck sweeper と retry_failed_ocr --ocr の両方が import して使用

### 改訂理由

- パイプライン分離に伴い、失敗段階を区別して差し戻す必要が出た
- stuck sweeper（process_ocr）と retry（retry_failed_ocr --ocr）でロジックが重複するため、共通モジュールに切り出して DRY 違反を回避

### 影響範囲

- §17 別表 B：retry_failed_ocr のサブコマンド体系追加
- cards/tasks/ocr_recovery.py 新規モジュール（仕様書に位置づけが必要）

### 確定経緯

2026-05-11 たんたんと B 側サポートクロード君で議論。BC 数による失敗段階判定（BC 0 件 = OpenCV 失敗 / BC ≥ 1 件 で ocr_status=failed = OCR 失敗）、共通ヘルパー切り出しを確定。

### 担当ブランチ

B 側(feature/v1.5.0-pipeline-split）

### マージタイミング

v1.4.2 リリース時

### ステータス

実装済み（コミット c944be2）

---

## #41 card_image の同期書きへの変更（save_card_image_tmp / on_commit リネーム廃止）

**対象仕様書**：統合最終版

### 項目名
切り出し画像の保存方式を、tmp ファイル + transaction.on_commit リネームから、同期書きに変更。

### 現状仕様（仕様書の該当章節）

- §8.5 パイプライン処理：BC を card_image=None で DB 先行作成 → save_card_image_tmp で tmp 拡張子で書き込み → transaction.on_commit で tmp → final にリネーム + BusinessCard.card_image を UPDATE

### 改訂内容

新方式：

- BC 作成と同タイミングで card_image を最終パス `cards/YYYY/MM/DD/<oid>-<idx>.jpg` に **同期書き**
- BC.objects.create(card_image=final_rel) を atomic 内で書く
- 書き込み失敗時：BC を card_image=None で作成、OriginalImage.error_message に「card_index=N: 切り抜き失敗 (理由)」を追記（v1.2.1 ルール踏襲）
- BC 作成失敗時：書き込み済みファイルを os.unlink でクリーンアップ

save_card_image_tmp / on_commit リネーム方式は **廃止**（→ #42 と一緒に削除）。

### 改訂理由

- パイプライン分離（→ #38）で OpenCV cron が card_image を確定書きしてから OCR cron に渡す構造になったため、tmp + on_commit リネームの利点がなくなった
- 同期書きの方が読みやすい・デバッグしやすい

### 影響範囲

- §8.5 切り出し処理仕様
- cards/tasks/card_cropper.py：save_card_image_tmp 関数削除、save_card_image を新パイプラインで使用

### 確定経緯

2026-05-11 コード君B 観察報告で「on_commit リネーム方式は v1.4.2 では捨てて同期書きに統一できる」が判明、確定。

### 担当ブランチ

B 側（feature/v1.5.0-pipeline-split）

### マージタイミング

v1.4.2 リリース時

### ステータス

実装済み（同期書き：コミット 65759d8、save_card_image_tmp 削除：コミット 3c76348）

---

## #42 旧 process_pending / PipelineCoordinator の完全削除

**対象仕様書**：統合最終版

### 項目名
v1.4.2 改訂前の 1 本パイプライン関連コードを v1.4.2 改訂で完全削除。

### 現状仕様（仕様書の該当章節）

- §8.5.4「process_pending」管理コマンド
- §8 PipelineCoordinator クラス（OpenCV + OCR + Contact 生成の直列処理）
- §15.1 cards 処理ルール（PipelineCoordinator 経由の処理が前提）

### 改訂内容

v1.4.2 で以下を **完全削除**：

| 対象 | 扱い |
|---|---|
| cards/management/commands/process_pending.py | ファイル削除 |
| cards/tasks/pipeline_coordinator.py（PipelineCoordinator クラスごと） | ファイル削除 |
| cards/tasks/card_cropper.py の save_card_image_tmp 関数 | 関数削除（save_card_image は新パイプライン用に残す） |

仕様書側：

- §8.5.4「process_pending」章を廃止 → 新章「process_opencv / process_ocr」（→ #38 / #39）に差し替え
- PipelineCoordinator 関連の記述をすべて削除
- §15.1 cards 処理ルールは「process_cardimage_with_ocr 内で処理される」前提に書き換え

### 改訂理由

- パターンA 撤回に伴い、1 本パイプラインの存在意義が消失
- v1.4.2 は新規ブランチ上の段階的作業で、main マージ時に cron 切替も同時実施するため deprecation 期間不要
- 旧コードを残すと「OpenCV + OCR を直列で 1 本にまわす経路」が共存し、v1.4.2 確定方針（パターンA 撤回・BC が raw_json 持つ）と挙動が矛盾するコードが残ってしまう

### 影響範囲

- §8 パイプライン章全体の書き換え
- §15.1 cards 処理ルールの書き換え
- §17 別表 B：process_pending の行削除

### 確定経緯

2026-05-11 たんたんと B 側サポートクロード君で議論。案 A（完全削除一発）／案 B（警告付き残置）／案 C（A + 運用 docs）から **案 A** で確定。

### 担当ブランチ

B 側（feature/v1.5.0-pipeline-split）

### マージタイミング

v1.4.2 リリース時。main マージ時に cron も `process_opencv` / `process_ocr` の 2 本に切替必須。

### ステータス

実装済み（コミット 3c76348、3 ファイル削除、合計 689 行 deletions）

---

## #43 UI 改修（7値フィルタ、ocr_status バッジ、andypf-json-viewer 採用、OriginalImage.raw_json 表示廃止）

**対象仕様書**：統合最終版

### 項目名
v1.4.2 のモデル拡張・パイプライン分離に伴う UI 改修一式。

### 現状仕様（仕様書の該当章節）

- §11 UI 仕様：CardListView の 5 値フィルタ（→ #15 で確定）
- §11 UI 仕様：CardDetailView の raw_json 表示（旧 `<pre>` ベース）
- §11 UI 仕様：OriginalDetailView の OCR 結果 raw_json viewer セクション
- §11 UI 仕様：ocr_result_badge カスタムタグ（→ #14 で確定）

### 改訂内容

**(a) CardListView フィルタ拡張（5 値 → 7 値）**

- 既存 5 値：business_card / not_business_card / insufficient_info / ocr_failed / others
- 追加 2 値：`_pending`（OCR 待ち）／ `_processing`（OCR 中）
- 仮想値 prefix `_` は OcrResult の実値と衝突回避のため
- フィルタクエリは _pending / _processing は `Q(ocr_status=...)`、既存 5 値は `Q(ocr_result__in=...)` に内部変換

**(b) ocr_result_badge カスタムタグ拡張**

- `BC.ocr_status` を見て分岐：
  - `pending` → 「OCR 待ち」バッジ（既存 `app-status-badge--muted`）
  - `processing` → 「OCR 中」バッジ（既存 `app-status-badge--info`）
  - `done` / `failed` → 既存の 5 値表示ロジック
- 新規 CSS クラス・JS は追加なし（既存クラスのみ使用）

**(c) OriginalDetailView の raw_json viewer 廃止**

- 旧セクション 8「OCR 結果 raw_json viewer」を削除
- 生 JSON 参照は BC 単位の名刺詳細リンクから

**(d) CardDetailView の OCR raw_json 表示を andypf-json-viewer に変更**

- 旧 `<pre>` + コピーボタンを廃止
- 1 回目 / 2 回目 raw_json それぞれを `<andypf-json-viewer>` カスタム要素で表示
- ライブラリは OriginalDetailView の debug_json viewer と同じ `@andypf/json-viewer@2.4.0`（CDN）
- 初期状態：折りたたみ（`expanded="0"`）
- 検索・コピー・サイズ表示は viewer 内蔵ツールバーで提供

**(e) admin の readonly 整理**

- OriginalImageAdmin から raw_json を削除（v1.4.2 で読み出し廃止）
- BusinessCardAdmin に raw_json_1 / raw_json_2 / ocr_status / claimed_at / error_message を readonly 追加
- list_display / list_filter に ocr_status / ocr_result 追加

### 改訂理由

- モデル拡張・パイプライン分離に伴う UI 側の整合性確保
- リッチ JSON viewer 化でユーザーがフィールド単位で展開・検索・コピーできるようにする

### 影響範囲

- §11 UI 仕様の各 view 章
- §20「views 設計」関連章
- 別表 D（カスタムタグ一覧）：ocr_result_badge の挙動更新

### 確定経緯

- 2026-05-11 たんたん追加確認3「ocr_status バッジ新設」確定
- 2026-05-11 たんたんから JSON viewer 化の要望、andypf-json-viewer 統一で確定

### 担当ブランチ

B 側（feature/v1.5.0-pipeline-split）

### マージタイミング

v1.4.2 リリース時

### ステータス

実装済み（コミット 0dd8e5f、7b467ac（テンプレートコメント修正）、2c35db0（andypf-json-viewer 採用））。自宅 PC で目視確認済み。

---

## #44 データマイグレーション 0011（旧 OriginalImage.raw_json → BC.raw_json_1 移行）

**対象仕様書**：統合最終版

### 項目名
v1.4.2 移行時、既存データの OriginalImage.raw_json を BC.raw_json_1 に移し替えるデータマイグレーション。

### 現状仕様（仕様書の該当章節）

- 既存マイグレーション 0001〜0009（v1.4.2 改訂前の段階的拡張）
- 別表 D マイグレーション一覧（仕様書にあれば）

### 改訂内容

新規マイグレーション：

- 0010：スキーママイグレーション（→ #33 + #34、自動生成）
- **0011：データマイグレーション（手書き）**

データマイグレーション 0011 の挙動：

- 全 BC をループ
- `bc.original_image.raw_json` が None または `cards` キー無しなら skip
- `raw_json["cards"][bc.card_index]` を取り出し、`{"cards": [<card_data>], "api_response": <raw_json.get("api_response", {})>}` の形に組み直して `bc.raw_json_1` に格納
- `bc.raw_json_2` は null
- `bc.ocr_status = "done"` 固定（既存 BC は OCR 完了済み前提）
- `bc.ocr_result` は既存値維持
- 例外時はその BC をスキップしてログ出力、他は続行
- reverse は no-op

### 改訂理由

- v1.4.2 で生 JSON の保存単位を OriginalImage → BC に変更（→ #35）
- 既存データを救済する必要あり

### 影響範囲

- マイグレーション一覧（別表 D 等）に 0010 / 0011 を追加
- 移行後の旧フィールド（OriginalImage.raw_json）の扱いは #35 で「物理残置」確定

### 確定経緯

2026-05-11 たんたん追加確認4「データマイグレーション必須」確定

### 担当ブランチ

B 側（feature/v1.5.0-pipeline-split）

### マージタイミング

v1.4.2 リリース時。マイグレーション番号は A 側との衝突調整あり。

### ステータス

実装済み（コミット 9ce638c）。コード君B のサンドボックスで実 DB に BC=0 件のため対象 0 件で完走確認、本番データでの動作は本番デプロイ時に要確認。

---

# A 側追加実装項目（#46〜#49）

以下は A 側ブランチ（feature/v1.4.2-models）における追加実装（2026-05-11 以降）に伴う仕様改訂項目である。サポート担当クロード君からの 2026-05-11 投下プロンプトで一括依頼された 4 件（#33 / #34 / #35 / #36 の仮番号を、現行ストック採番に整合させて #46 / #47 / #48 / #49 として収録）。すべて実装完了済みで仕様書側の記述追加・修正のみ残る。

なお #45 は欠番（本セクション追加時の採番運用上、欠番のまま処理。詳細は更新履歴の第12版エントリを参照）。

---

## #46 ContactUpdateForm 動的確認 CB の追加条件「confirmed_at is None 限定」明文化

**対象仕様書**：統合最終版

### 項目名
ContactUpdateForm における動的な確認チェックボックス追加対象を「low/mid 信頼度 かつ confirmed_at is None」のフィールドに限定する仕様を明文化する。

### 現状仕様（仕様書の該当章節）

**§11.6.2 ContactUpdateForm**：low/mid フィールドに確認 CB を動的追加すると記述。

**§11.7.1 ContactUpdateView の修正画面の確認チェック**：同様の記述。

ただし「既に confirmed_at が記録済みのフィールドは追加対象外」という条件の明記がない。仕様書の文言上は「low/mid」だけが追加対象に見える。

### 改訂内容

**(1) §11.6.2 ContactUpdateForm 節**

動的追加の説明に以下を明記：

- 動的追加対象は **low/mid かつ `confirmed_at is None`** のフィールドのみ
- 既に `confirmed_at` が記録された（過去に確認済みの）フィールドは追加対象外

**(2) §11.7.1 ContactUpdateView の修正画面の確認チェック節**

(1) と同じ条件を明記。両節の表現を統一する。

### 改訂理由

実装（`contacts/forms.py` ContactUpdateForm.\_\_init\_\_）では「低/中信頼度 かつ 未確認」の条件で動的追加されているが、仕様書の文言では「low/mid」だけが追加対象に見える。再実装時の齟齬を防ぐため明文化が必要。サポート担当クロード君が ContactUpdateForm 実装過程で実装・仕様書の食い違いを発見し、たんたん判断で「仕様書を実装に合わせる方向で明文化」と確定。

### 影響範囲

- §11.6.2 ContactUpdateForm 節（動的追加条件の記述）
- §11.7.1 ContactUpdateView の修正画面の確認チェック節（同条件の記述）
- 両節の表現統一

### 確定経緯

2026-05-11 サポート担当クロード君から仕様書改訂依頼として投下。実装はコミット 151f432 で完了（ステップ1）。

### 担当ブランチ

A 側（feature/v1.4.2-models）

### ステータス

実装済み（コミット 151f432 で push 済み）。仕様書側の記述追加のみ残っている。

---

## #47 Contact.lang に blank=True 明文化

**対象仕様書**：統合最終版

### 項目名
Contact.lang フィールドの定義に `blank=True` を明文化する。

### 現状仕様（仕様書の該当章節）

**§4.x Contact モデル定義表**：`lang` フィールド行に `blank=True` の記載がない。

### 改訂内容

Contact モデル定義表の `lang` 行の属性カラムに **`blank=True`** を追加。

### 改訂理由

OCR 結果で lang が判定できないケース、ユーザー入力で空のまま保存するケースに対応するため、`blank=True` が必須。コミット 32ef99d で実装済みだが仕様書本体に未反映。

### 影響範囲

- §4.x Contact モデル定義表（`lang` フィールド行）
- マイグレーションは実装済み（コミット 32ef99d）

### 確定経緯

2026-05-11 サポート担当クロード君から仕様書改訂依頼として投下。実装はコミット 32ef99d で完了。

### 担当ブランチ

A 側（feature/v1.4.2-models）

### ステータス

実装済み（コミット 32ef99d で push 済み）。仕様書側の記述追加のみ残っている。

---

## #48 §10.4.3 行 1117 削除（fix での set_primary_contact 記述、§11.4.1 との矛盾解消）

**対象仕様書**：統合最終版

### 項目名
§10.4.3「Person.set_primary_contact() の詳細仕様」の「呼ばれる場所」リストから、`ContactUpdateView の change_reason='fix' のとき set_primary_contact(new_contact) または set_primary_contact(new_contact, old_primary_new_status='active') を呼ぶ`という記述（行 1117 周辺）を削除する。

### 現状仕様（仕様書の該当章節）

**§10.4.3「Person.set_primary_contact() の詳細仕様」の「呼ばれる場所」リスト（行 1117 周辺）**：

> 修正画面 ContactUpdateView（change_reason='fix' のとき）：`person.set_primary_contact(new_contact)` または `person.set_primary_contact(new_contact, old_primary_new_status='active')`

一方、**§11.4.1「修正理由による処理分岐」（行 1517〜）**：

| 修正理由 | 内部処理 |
|---|---|
| fix | 既存 Contact を更新（`contact.fix(form, user)` 経由） |
| transfer / promotion / job_change / name_change | 新規 Contact 作成 + 既存を inactive に |

§11.4.1 では fix のとき「新規 Contact を作らない（既存を上書きするだけ）」と明記されている。

### 改訂内容

**(1) §10.4.3 行 1117（fix での set_primary_contact 呼び出し記述）の削除**

該当 1 行を削除。修正画面 ContactUpdateView の transfer 系（行 1118 想定）の記述は維持。

**(2) 併せて検討すべき点：§10.4.3 の `old_primary_new_status` 表の整理**

§10.4.3 の `old_primary_new_status` 表で `'active'` の使用場面に「修正画面 fix 系」と記載がある場合、これも整理対象（fix では set_primary_contact を呼ばなくなるため、`'active'` の使用場面が空になる可能性）。

**将来検討事項**：`'active'` をデフォルト値として残すか、`'inactive'` のみに統合するかは仕様書全体の整合性を確認して判断する（仕様書本体への反映フェーズで実施）。

### 改訂理由

§11.4.1「修正理由による処理分岐」では、change_reason='fix' のとき「既存 Contact を更新（`contact.fix(form, user)` 経由）」と明記されており、**新規 Contact を作らない**。新規 Contact が存在しない fix で `set_primary_contact(new_contact)` を呼ぶことはできない（`new_contact` 引数に渡すものがない）。

§10.4.3 行 1117 の記述は §11.4.1 確定前の旧仕様の残骸と判定。業務的にも「田中太朗 → 田中太郎」の誤字訂正で Contact レコードが増殖するのは不自然であり、§11.4.1 の解釈（既存 Contact 1 つのフィールド値を上書きするだけ）が業務的妥当性を持つ。

### 影響範囲

- §10.4.3「Person.set_primary_contact() の詳細仕様」の「呼ばれる場所」リスト（行 1117 周辺）
- §10.4.3 の `old_primary_new_status` 表（`'active'` の使用場面記述、要整理）
- §11.4.1 修正理由による処理分岐表（変更なし、本記述は維持）
- 実装側は §11.4.1 を正として実装済み（`contact.fix(form, user)` で完結、set_primary_contact 呼び出しなし）

### 確定経緯

2026-05-11 サポート担当クロード君が ContactUpdateView 実装着手前に §10.4.3 と §11.4.1 の食い違いを発見、論点としてたんたんに案 A / B / C を提示。

- 案 A：§11.4.1 が正、§10.4.3 行 1117 は旧仕様の残骸として削除、実装は `contact.fix(form, user)` で完結
- 案 B：§10.4.3 が正、fix でも新規 Contact 作成（誤字訂正でレコード増殖を許容）
- 案 C：オーパス君に「§10.4.3 と §11.4.1 のどちらが意図か」確認依頼してから実装

たんたん判断で **案 A 確定**（誤字訂正で Contact レコード増殖は業務的に不自然との判断）。実装はコミット 1caec0a で完了（ステップ4、§11.4.1 を正として実装）。

### 担当ブランチ

A 側（feature/v1.4.2-models）

### ステータス

実装済み（コミット 1caec0a で push 済み、§11.4.1 を正として実装、`contact.fix(form, user)` で完結）。仕様書側の §10.4.3 行 1117 削除と `old_primary_new_status` 表整理のみ残っている。

---

## #49 15 番 DuplicateCandidateGroupListView 絞り込み仕様 + サイドバー追加の明文化

**対象仕様書**：統合最終版

### 項目名
15 番 DuplicateCandidateGroupListView（重複候補グループ一覧画面）の絞り込み仕様、ならびにサイドバーへのリンク追加を仕様書本体に明文化する。

### 現状仕様（仕様書の該当章節）

**§11.3 URL 一覧表 15 番**：DuplicateCandidateGroupListView の備考列に簡素な記述のみ。絞り込み仕様の詳細記載なし。

**§11.5 ビュー層の設計**：DuplicateCandidateGroupListView の節がない、または詳細記述なし。

**§20 サイドバー関連**：サイドバーメニュー構成の節があれば該当箇所に重複候補一覧リンクの記載なし。

### 改訂内容

**(1) §11.3 URL 一覧表 15 番の備考列を拡張**

備考列を以下に変更：

> 重複候補グループ一覧。group_id 単位で集約表示。絞り込みフォーム（rank の複数選択 / 進捗の複数選択 / ユーザー絞り込み）あり。

**(2) §11.5 に新規節「DuplicateCandidateGroupListView の絞り込み仕様」を追加**

具体的な仕様：

- **rank フィルタ**：4 値（exact_match / possible_high / possible_mid / possible_low）の複数選択。初回（`searched` 未指定）はデフォルトで全 rank
- **progress フィルタ**：「未レビュー（pending）」「完了（completed）」の複数選択。初回はデフォルトで「未レビュー」のみ
- **user フィルタ**：チェックボックス 1 つ（`user=me`）。ON のとき `person_a.primary_contact.created_by` または `person_b.primary_contact.created_by` がログインユーザーに一致するペアの group のみ（OR 条件、自分が読み取った Person を含む候補を抽出）
- **並び順**：未レビュー優先（pending_count > 0 を先）→ rank 順（exact_match → possible_low）→ group_id 順
- **group_id IS NULL のレコードは集約対象外**（単発候補は本画面の対象外）

新規節の番号採番はオーパス君判断（仕様書本体への反映フェーズで適切な位置を選ぶ）。

**(3) §20 サイドバー関連節への追記**

サイドバー（`templates/cards/_sidebar.html`）に「重複候補一覧」リンクを追加し、`active_menu="duplicates:duplicate_group_list"` のとき `is-active` を付与する。仕様書にサイドバーメニュー構成の節があればそこに反映（該当節はオーパス君判断）。

### 改訂理由

D-4e 実装（コミット 4f605c8）で 15 番 View / テンプレート / 絞り込み / サイドバーリンクを実装したが、仕様書本体には簡素な記述しかなく、再実装時 / レビュー時に齟齬が発生するリスクがある。とくに「ユーザー絞り込み」「サイドバーへのリンク追加」は v9 セッションでたんたん判断で追加した仕様書外の要件であり、仕様書本体への明文化が必須。

### 影響範囲

- §11.3 URL 一覧表 15 番（備考列拡張）
- §11.5 ビュー層の設計（新規節「DuplicateCandidateGroupListView の絞り込み仕様」を追加）
- §20 サイドバー関連節（該当節があればリンク追加記述。該当節はオーパス君判断）
- 別表 A.6 URL 一覧表（PDF 側、参照箇所のみ）

### 確定経緯

2026-05-11 サポート担当クロード君から仕様書改訂依頼として投下。実装はコミット 4f605c8 で完了（D-4e）。ユーザー絞り込み・サイドバーリンク追加は v9 セッションでたんたん判断で追加した仕様書外の要件。

### 担当ブランチ

A 側（feature/v1.4.2-models）

### ステータス

実装済み（コミット 4f605c8 で push 済み）。仕様書側の記述追加のみ残っている。

---

# 将来検討メモ（仕様書改訂対象外）

以下は仕様書改訂の対象外として記録のみ残す項目。

## 情報メモ：record_*_action のシグネチャ修正経緯（X-5 関連）

**対象**：統合最終版 §10.7.2 / §10.8.2

**メモ内容**：

`record_merge_action` / `record_different_person_action` のシグネチャは **user 引数のみ** が正しい（仕様書がもともと正しい）。

ただし、A-2d 実装段階で前任クロード君が独自判断により追加引数 3 つを追加していたため、X-5（コミット ad11ace）で仕様書通りの user のみに戻す修正を実施した。

**仕様書側修正は不要**（仕様書がもともと正しい）。本メモは「実装が一時的に仕様書から逸脱し、X-5 で戻した」という経緯の記録として残す。

**教訓**：仕様書から逸脱した引数追加・関数変更は、たんたん確認なしに指示書に書かない。「過去のクロード君が決めた」という記述だけで実装承認しない。仕様書と実装の食い違いを見つけたら、まず conversation_search で過去議論を確認してから報告する。

---

## UI 担当依頼候補（4 件）

以下は B 側サポート担当クロード君の報告書（2026-05-08）で「スコープ外として残した課題」として明示された項目。本ストックの仕様書改訂対象には含めず、将来 UI 担当に依頼するときの根拠として記録のみ。

| # | 内容 | 関連ストック |
|---|---|---|
| F-1 | 元画像詳細セクション7 の「切り抜き画像（あり/なし）」列の削除（サムネ列追加で冗長化） | #18 |
| F-2 | 名刺一覧画面の「無効名刺だけ一発選択」UI（5 値別チェックではなく select 形式や全選択ボタン） | #15 |
| F-3 | 削除ボタンと戻るボタンの並び順・spacing 調整 | #17 |
| F-4 | ocr_result_badge の表示位置の最適化（現状は orientation バッジと横並び、将来的に右上 / 左下のいずれかに固定する検討） | #14 |

## DEBUG=False 時の挙動メモ（v1.4.x スコープ外）

動作確認時に発覚：DEBUG=False にすると media ファイルが配信されずサムネが壊れる（Django 標準動作）。本番環境想定のデプロイ時に urls.py / nginx 等の整理が必要。これは v1.4.x スコープ外で、本番デプロイ準備フェーズで考える話。仕様書反映の対象外。

---

# マージ後反映候補（B 側ブランチ由来）

以下は B 側ブランチ（feature/opencv-improvement）の改修内容で、本流（v1.4.2）にマージされた後に仕様書反映する候補である。マージ前に仕様書を変えると A 側の認識と齟齬が出るため、本ストックには参考として列挙のみとする。

| # | 項目 | 状態 |
|---|---|---|
| B-1 | OriginalImage に debug_json (JSONField, null=True, blank=True) フィールド追加 ※#13 DebugMask 新設に伴い、debug_json は `mask_white_ratios` キーを持たない（DebugMask.metadata に格納）構造に整理される | B 側ブランチで実装済み（mask_white_ratios 削除は #13 と同じマイグレーション 0008 で対応） |
| B-2 | detect_cards_with_debug() 関数の追加（cards/services/detectors/opencv_detector.py） | B 側ブランチで実装済み |
| B-3 | /originals/<uuid:pk>/recalc-debug/ URL（POST 専用）追加 | B 側ブランチで実装済み |
| B-4 | OriginalDetailView の改修（ステップ1C-2） | B 側ブランチで実装済み |
| B-5 | 巨大 polygon 警告、統計サマリー磨き込み、検出失敗警告（ステップ1D-1） | B 側ブランチで予定 |
| B-6 | フェーズ2 OpenCV 関連：process_pending --opencv-only フラグ追加（コミット bf78d0b）、recalc_opencv_debug を cards/services/opencv_debug_cache.py に新設、元画像一覧画面のUI改善（9列構成、annotate + Conditional Aggregation で N+1 回避） | B 側ブランチで実装済み |

これらは B 側がマージされてから、別途まとめて仕様書反映を検討する。

**注**：ストック #1／#9〜#18／#20 は B 側ブランチでの実装ではあるが、本流（v1.4.2 仕様）に直接影響する仕様変更（モデル定義の追加・変更、ビュー層の追加・変更を含む）のため、上記「マージ後反映候補」とは区別してストック本体に記載している。

---

# 改訂後の整合性チェックリスト（実装完了時に確認）

最終的な改訂提案メモ作成時に、以下の整合性を確認する。チェックリストは対象仕様書ごとに分けて整理する。

## 統合最終版（名刺画像取り込みOCR仕様書_v1_4_2統合最終版.md）

- [ ] #1 BC 残置仕様に伴う、§15.1 / §4.3 / §4.4 / §14.4 / 別表 C / 別表 A.4 の整合性（→ #9 / #10 / #11 / #12 と統合確認）
- [ ] #2 削除に伴う、§4.7 / §14.4 / 別表 A.8 の整合性
- [ ] #3 アプリ独立に伴う、§4.10 / §4.11 / §10.9 / §11.1 / §11.2 / 別表 A.1 / A.2 の整合性
- [ ] #4 / #5 に伴う、§4.10 / §4.10.1 / §4.11 / §10.9 / §12.10 / 別表 A.10 の整合性
- [ ] #6 に伴う、別表 C.1 と本文中の OriginalImage.Status 言及箇所の整合性
- [ ] #7 に伴う、§8.3 / §8.4 / §8.10 の整合性
- [ ] #8 に伴う、§8.7 と実装側の整合性
- [ ] #9 BusinessCard.OcrResult 追加に伴う、§4.3 / §14.4 / 別表 C / 別表 A.4 / マイグレーションの整合性
- [ ] #10 §15.1 改訂に伴う、§15.1 / §4.3 / 別表 A.4（card_image 説明補足）の整合性
- [ ] #11 BC ↔ Contact 関係性明文化に伴う、§4.3 / §4.4 / §4.4.0 / §4.4.1 の整合性、および BC 削除カスケード仕様（#17 関連）の整合性
- [ ] #12 detected_count 定義明文化に伴う、§4.2 / 別表 A.3 の整合性
- [ ] #13 DebugMask 新設に伴う、§4 新規節 / §14.4 / 別表 A.2 / 別表 A.X（DebugMask フィールド対照表）/ 別表 C / マイグレーション 0008 の整合性、および「DB 1 次ソース、FS 従属物」設計思想の本文記載の整合性
- [ ] #14 ocr_result_badge カスタムタグ追加に伴う、§11.8.1 / §11.8.2 / §11.8 冒頭文 / §11.2 ディレクトリ構成の整合性
- [ ] #15 CardListView ocr_result フィルタ追加に伴う、第13章 / templates/cards/card_list.html / BackNavigator 保持パラメータの整合性
- [ ] #16 CardDetailView sibling_cards 通常表示化に伴う、第13章 / templates/cards/card_detail.html の整合性
- [ ] #17 CardDeleteView 追加に伴う、第13章 / §11.2 / URL 一覧 / 削除カスケード仕様（#11 と統合）の整合性
- [ ] #18 OriginalDetailView セクション7 改修に伴う、第13章 / templates/cards/original_detail.html の整合性
- [ ] #20 設計方針大転換に伴う、§9.3.1 / §9.4.4 / §9.4.5 / §11.5.5 / §11.5.6 / §11.5.7 / §11.6.2 / §13.4 の整合性（廃止確定項目の削除と、新規仕様の追加が D-3 完了後に必要）
- [ ] #21 mark_as_active() 追加に伴う、§10.4.1 / §9.5.2 ステップ5 / 第13章 Execute_Merge_Undo フロー記述の整合性
- [ ] #22 §12.8.3 / §9.3.1 / §9.5.2 修正に伴う、recover 責務・mark_as_merged 呼び出し順序・復元時 duplicate_checked_at リセット手順の整合性
- [ ] #23 transfer_contacts_to 挙動明文化に伴う、§10.4.1 / §9.4 / §9.4.3 / 別添 PDF の対応関係の整合性
- [ ] #24 PersonDetailView 役割変更に伴う、§11.3 / §11.4 / §11.2 / §10.4 / 別表 A.6 の整合性、および archived な Person への対応（§10.4.2 `Person.get_archived()` との整合）
- [ ] #25 ContactDetailView 業務メイン画面化に伴う、§11.3 / §11.4 / §11.8 / 別表 A.6 / §10.5 の整合性、および編集可能モード判定の View 層ガード（D-3c AJAX エンドポイント）との整合
- [ ] #27 Contact.full_name 空文字制約追加に伴う、§4.4.1 / §15.5.3 / Migration / contacts/views.py / Form clean メソッドの整合性
- [ ] #28 CardDetailView 業務画面化に伴う、§11.3 / §11.4 / §11.8 / §13 章（または該当節） / 別表 A.6 の整合性、および 3 詳細ビューの業務的位置づけ記述の整合性
- [ ] #29 ContactListView 新規追加に伴う、§11.3 URL 一覧表（一覧画面なしリストとテーブル）/ §11.4 ContactListView 節新設 / §20.1 削除 / §18.2 認証方針との整合 / サイドバーメニュー記述（該当節）の整合性
- [ ] #30 ContactDetailView inactive Contact 履歴セクション追加に伴う、§11.4 ContactDetailView 節 / §11.8 共通 include の節（該当箇所）/ contact_detail.html / 共通 include `_inactive_contacts.html` / persons 側 include パスの整合性
- [ ] #31 ContactDetailView 現在所属 Person 遷移リンク追加に伴う、§11.4 ContactDetailView 節 / contact_detail.html の整合性（実装済み：操作ボタンセクション先頭、is_editable 条件外に独立配置、orphan Contact では `{% if contact.person %}` ガード、コミット fdb3805）
- [ ] #46 ContactUpdateForm 動的確認 CB の「confirmed_at is None 限定」明文化に伴う、§11.6.2 / §11.7.1 の整合性、および両節の表現統一
- [ ] #47 Contact.lang blank=True 明文化に伴う、§4.x Contact モデル定義表の整合性
- [ ] #48 §10.4.3 行 1117 削除に伴う、§10.4.3「呼ばれる場所」リストの整合性、および §11.4.1 修正理由による処理分岐表との矛盾解消の最終確認
- [ ] #48 §10.4.3 の `old_primary_new_status` 表整理（fix で set_primary_contact 呼ばなくなるため `'active'` の使用場面が空になる可能性、`'active'` をデフォルト値として残すか `'inactive'` のみに統合するかは仕様書全体の整合性を確認して判断）
- [ ] #49 15 番 DuplicateCandidateGroupListView 絞り込み仕様 + サイドバー追加の明文化に伴う、§11.3 URL 一覧表 15 番備考列 / §11.5 新規節「DuplicateCandidateGroupListView の絞り込み仕様」/ §20 サイドバー関連節（該当節） / 別表 A.6 の整合性
- [ ] #9 と #14 の連動確認（OcrResult TextChoices と ocr_result_badge カスタムタグの対応）
- [ ] #14 と #15 / #16 / #18 の連動確認（ocr_result_badge を使う各画面の整合性）
- [ ] #20 と #21 / #22 / #23 の連動確認（マージ系処理の設計大転換と、その周辺メソッド・フローの整合性）
- [ ] #24 と #25 の連動確認（PersonDetailView 役割変更と ContactDetailView 業務メイン画面化が業務フローとして一貫しているか）
- [ ] #25 と #28 と #29 の連動確認（3 詳細ビュー ContactDetailView / CardDetailView / PersonDetailView と新規 ContactListView の業務的位置づけが一貫しているか、編集可能モード判定式が 3 ビューで完全一致しているか）
- [ ] #30 と #25 の連動確認（ContactDetailView マージ関連セクション構成要素として inactive Contact 履歴が #25 で記述したセクション構成と整合しているか）
- [ ] #31 と #24 の連動確認（PersonDetailView active 分岐リダイレクト設計（#24）と現在所属 Person 遷移リンク（#31）が業務フローとして一貫しているか）
- [ ] #46 と #25 の連動確認（ContactDetailView 編集可能モード判定式と ContactUpdateForm 動的追加条件の整合性）
- [ ] #48 と #22 の連動確認（マージ復元処理フローと set_primary_contact 呼び出し記述の整合性、行 1117 削除後の §10.4.3 全体の自然さ）

## v0.1.5 詳細（Run_Generate_Duplicate_Candidates_詳細仕様書_v0_1_5.md）

- [ ] #19 X-3 ランナバグ修正に伴う、§3.3 / §3.4 / §3.6 / §3.6.1 / §5.3.2 / §5.4.1 / §6.2 の整合性
- [ ] #19 §3.3 への手順 (3.5) 挿入に伴う、後続手順の番号採番（または全番号振り直し）の判断
- [ ] #19 統合最終版への波及がないことの最終確認

## BackNavigator ガイド（BackNavigator使い方ガイド.docx）

- [ ] #26 §9 画面遷移イメージの記述修正（push_current(title, []) の例を実装慣例に合わせる）
- [ ] #26 §8 NG パターン（「keys を空にする」NG パターン記述）との整合性確認

## OCR / OpenCV パイプライン分離関連（B 側マージ時に統合反映、対象は統合最終版）

以下は B 側ブランチ（feature/v1.5.0-pipeline-split、ブランチ名は履歴的事実）が main にマージされる際にまとめて仕様書反映する項目群。A 側マージ完了後に整合性確認・衝突調整を行う前提。すべて v1.4.2 リリースの範囲。

- [ ] #32 パターンA 撤回に伴う、v1.2.0 主要方針章 / §8.5 処理パイプライン全体の整合性
- [ ] #33 BusinessCard モデル拡張に伴う、§4.3 / §14.4 / 別表 C / 別表 A.4 / マイグレーション 0010 の整合性、および ocr_result null 許容化と #9（5 値 default=business_card）との整合
- [ ] #34 STATUS_CHOICES 拡張に伴う、§4.2 / §14.4 / 別表 C.1（5 値 → 7 値）/ #43 CardListView 7 値フィルタとの整合性、および状態遷移図の最終確認
- [ ] #35 OriginalImage.raw_json 読み出し廃止に伴う、§4.2 / §11 UI 仕様 / admin / #44 データマイグレーション 0011 との整合性、および「物理残置」記述の明文化
- [ ] #36 パイプライン関数構造刷新に伴う、§8 パイプライン章 / §13 関数命名規則 / §8.5.4 Extract_Cards_via_OCR 廃止記述の整合性
- [ ] #37 条件付き 2 回 OCR に伴う、§8 新章「条件付き 2 回 OCR」/ §13 / §15 cards 処理ルール（採用 raw_json 選択ロジック）の整合性、および orientation → 補正回転マップの記述
- [ ] #38 process_opencv 新設に伴う、§8.5 OpenCV cron セクション / §17 別表 B / 運用 docs の整合性、および「BC が 1 件でも存在すれば再実行禁止」の明文化
- [ ] #39 process_ocr 新設に伴う、§8.5 OCR cron セクション / §17 別表 B / 運用 docs の整合性、および OriginalImage.status 集計遷移ロジックの記述
- [ ] #40 retry_failed_ocr 分岐と ocr_recovery.py に伴う、§17 別表 B / cards/tasks/ocr_recovery.py の位置づけ記述の整合性
- [ ] #41 card_image 同期書きに伴う、§8.5 切り出し処理仕様 / cards/tasks/card_cropper.py 記述の整合性
- [ ] #42 旧 process_pending / PipelineCoordinator 削除に伴う、§8 / §15.1 / §17 別表 B の書き換え整合性
- [ ] #43 UI 改修一式に伴う、§11 UI 仕様各 view 章 / §20 views 設計章 / 別表 D（カスタムタグ一覧）の整合性、および #15 / #14 との統合反映
- [ ] #44 データマイグレーション 0011 に伴う、マイグレーション一覧（別表 D 等）への 0010 / 0011 追加、A 側マイグレーションとの衝突調整完了確認
- [ ] #32 と #33 / #34 / #36 の連動確認（パターンA 撤回が BC モデル拡張・STATUS_CHOICES 拡張・関数構造刷新と整合しているか）
- [ ] #35 と #44 の連動確認（読み出し廃止とデータマイグレーションが整合しているか、物理残置の前提が崩れていないか）
- [ ] #36 と #37 と #38 / #39 の連動確認（関数構造と条件付き 2 回 OCR と 2 本 cron が業務フローとして一貫しているか）
- [ ] #38 と #39 と #40 の連動確認（2 本 cron と差し戻しコマンド・stuck sweeper の責務分担が整合しているか）
- [ ] #41 と #42 の連動確認（同期書き化と PipelineCoordinator 削除が同時に成立しているか）
- [ ] #43 と #34 と #33 の連動確認（UI 7 値フィルタと STATUS_CHOICES 7 値拡張、BC.ocr_status 連動が整合しているか）

---

# 更新履歴

| 日付 | 件数 | 更新内容 |
|---|---|---|
| 2026-05-07 | 8件 | 初版作成。サポート担当クロード君（A 側）からの引き継ぎ情報および B 側サポート担当クロード君からの伝言を統合。 |
| 2026-05-07 | 12件 | 第2版作成。#1（BC 残置仕様）の方針確定・要確認事項解消を反映。新規項目 #9（BusinessCard.ocr_result フィールド追加）／#10（§15.1 cards 処理ルール改訂）／#11（BC ↔ Contact 関係性明文化）／#12（detected_count 定義明文化）を追加。 |
| 2026-05-07 | 13件 | 第3版作成。#13（DebugMask モデル新設）を追加。これに伴い、マージ後反映候補 B-1 に「mask_white_ratios キーは持たない（DebugMask.metadata に格納）」を補足。 |
| 2026-05-08 | 18件 | 第4版作成。ステップ2（UI 改善）の実装完了報告（B 側サポートクロード君、コミット 3ceacb6）を反映。新規項目 #14（ocr_result_badge カスタムタグ）／#15（CardListView ocr_result フィルタ）／#16（CardDetailView sibling_cards 通常表示化）／#17（CardDeleteView 新規追加）／#18（OriginalDetailView セクション7 改修）を追加。#9 / #10 / #13 のステータスを「実装済み（コミット 3ceacb6）」に更新。#1 / #11 の関連項目に #14 / #17 への参照を追記。「将来検討メモ（仕様書改訂対象外）」セクションを末尾に新設し、UI 担当依頼候補 4 件と DEBUG=False 時の挙動メモを記録。 |
| 2026-05-08 | 19件 | 第5版作成。新規項目 #19（X-3 ランナバグ修正に伴う v0.1.5 詳細仕様書の改訂）を追加（A 側サポート担当クロード君からの依頼、コミット 4cd1c3e、対象は v0.1.5 詳細仕様書のみ、統合最終版への波及なし）。サマリー表に「対象仕様書」列を追加（統合最終版／v0.1.5 詳細の区別を明示）。整合性チェックリストを対象仕様書ごとに分割。本ファイル冒頭の対象仕様書記述を複数明記に改訂。 |
| 2026-05-10 | 23件 | 第6版作成。新規項目 #20（Contact 詳細画面 AJAX 化に伴う設計方針大転換、廃止確定 3 項目を包含）／#21（Person.mark_as_active() 新規追加、X-6 完了 / コミット cef83c4）／#22（§12.8.3 / §9.3.1 / §9.5.2 のマージ復元処理フロー記述修正、C-3 関連）／#23（Person.transfer_contacts_to() 挙動明文化、X-4 完了 / コミット 62a8ea9）を追加。「将来検討メモ」セクションに record_*_action シグネチャ修正経緯（X-5 / コミット ad11ace、仕様書修正不要だが経緯を記録）を情報メモとして追記。マージ後反映候補に B-6（フェーズ2 OpenCV 関連、コミット bf78d0b）を追加。整合性チェックリストに #20〜#23 関連項目を追加。 |
| 2026-05-10 | 26件 | 第7版作成。D-3 ブロック実装（A 側、コミット 0c8bfbe / 82957ee / fec32d4）に伴う新規項目 #24（PersonDetailView の役割変更、active は ContactDetailView へリダイレクト、merged / archived は専用詳細画面、方針確定）／#25（ContactDetailView の業務メイン画面化と新規セクション 2 つ追加、コミット fec32d4 で実装済み・テスト 60/60 PASS）／#26（BackNavigator 使い方ガイド §9 の push_current(title, []) 記述の実装非整合解消、共通担当）を追加。対象仕様書に「BackNavigator使い方ガイド.docx」を追加。整合性チェックリストに BackNavigator ガイド用セクションを新設し、統合最終版セクションには #24 / #25 関連項目および連動確認を追加。 |
| 2026-05-11 | 29件 | 第8版作成。ContactListView 実装完了および周辺仕様変更に伴う新規項目 #27（Contact.full_name の空文字制約追加、A 側、方針確定・未実装）／#28（CardDetailView の業務画面化、A 側、コミット c1c7686 で実装済み・cards 15/15 + contacts 65/65 PASS）／#29（ContactListView 新規追加、A 側、コミット 53ff061 で実装済み、§11.3 / §20.1 改訂）を追加。前任サポート担当クロード君の引継ぎ資料 v5 で「追記候補 5・6・7」「#20 / #21 / #22」と独自採番されていた 3 件を、現行ストック採番ルール（通し番号方式）に従い #27 / #28 / #29 に整合させた。整合性チェックリスト統合最終版セクションに #27 / #28 / #29 関連項目および #25 / #28 / #29 の連動確認を追加。 |
| 2026-05-11 | 31件 | 第9版作成。本セッション（2026-05-11）の追加実装に伴う新規項目 #30（ContactDetailView に inactive Contact 履歴セクション追加、マージ関連セクション内・共通 include 化、A 側、コミット 61cfd91 で実装済み・テスト 80 件 PASS）／#31（ContactDetailView に「現在所属 Person への遷移リンク」追加、A 側、方針確定・実装未着手、配置位置はストック段階では未確定として注記）を追加。整合性チェックリスト統合最終版セクションに #30 / #31 関連項目および #30 と #25 の連動確認・#31 と #24 の連動確認を追加。 |
| 2026-05-11 | 44件 | 第10版作成。B 側ブランチ（feature/v1.5.0-pipeline-split、起点 0e26a4c〜最終 2c35db0、全 12 コミット）における OCR / OpenCV パイプライン分離関連の新規項目 #32〜#44 を追加（13 件）。内容は OCR / OpenCV パイプライン分離（v1.2.0 パターンA 撤回、BC モデル拡張、STATUS_CHOICES 拡張、raw_json 読み出し廃止、パイプライン関数構造刷新、条件付き 2 回 OCR、process_opencv / process_ocr 新設、retry_failed_ocr 分岐 + ocr_recovery.py 共通化、card_image 同期書き、旧 process_pending / PipelineCoordinator 削除、UI 改修、データマイグレーション 0011）。第10版作成時点では #32〜#44 を「v1.5.0 関連」として v1.4.2（#1〜#31）と分離管理する方針で運用したが、第11版でたんたん方針確定により v1.4.2 統合管理に変更（下記第11版の行を参照）。整合性チェックリストに該当セクションを新設し、#32〜#44 関連項目と連動確認（#32 と #33 / #34 / #36 ／ #35 と #44 ／ #36 と #37 と #38 / #39 ／ #38 と #39 と #40 ／ #41 と #42 ／ #43 と #34 と #33）を追加。新規 #32〜#44 はすべて B 側ブランチ担当・対象仕様書は統合最終版。 |
| 2026-05-11 | 44件 | 第11版作成。たんたんから方針変更：「v1.5.0 という新バージョンは作らず、今回の OCR / OpenCV パイプライン分離も v1.4.2 の範囲として扱う。A 側（feature/v1.4.2-models）と B 側（feature/v1.5.0-pipeline-split）は両方 main にマージされて 1 つの v1.4.2 リリースとして出す」が確定。第10版で v1.5.0 関連として分離管理していた #32〜#44 を v1.4.2 統合管理に書き戻す修正を実施。具体的には(1) ファイル冒頭の本ファイルの目的記述を「v1.4.2 関連の仕様書」に戻す、(2) マージ・反映の運用方針セクションを「本ストックの項目（#1〜#44）はすべて v1.4.2 リリースの範囲として扱う」「A 側 → B 側のマージ順序前提、衝突調整は B 側マージ時に対応、両ブランチが main にマージされて 1 つの v1.4.2 リリースとして出す」に書き換え、(3) #32 サマリー表のステータス欄「実装済み（feature/v1.5.0-pipeline-split）」はブランチ名なので維持、(4) #32〜#44 各項目本文の「v1.5.0 で〜」「v1.5.0 以降」「v1.5.0 リリース時」「v1.5.0 で公式に撤回」「v1.5.0 では物理残置」「v1.5.0 関連」等の方針表現を「v1.4.2」に統一書き換え、(5) 整合性チェックリストの「v1.5.0 関連」セクション見出しを「OCR / OpenCV パイプライン分離関連」に変更。残した「v1.5.0」表記はブランチ名 `feature/v1.5.0-pipeline-split`（履歴的事実、複数箇所）と #32 確定経緯の「2026-05-08 当初 v1.5.0 据え置きで合意」「2026-05-11 v1.5.0 据え置きを覆して進める」（過去の議論経緯としての時系列上の事実）のみ。件数 44 件は変更なし、項目の追加・削除なし。 |
| 2026-05-11 | 45件 | 第12版作成。サポート担当クロード君（A 側）から 2026-05-11 投下プロンプトで一括依頼された A 側追加実装 5 件（仮番号 #31 / #33 / #34 / #35 / #36）に対応。仮番号 #31（ContactDetailView「現在所属 Person への遷移リンク」追加）は現行 #31 のステータス更新（方針確定・実装未着手 → 実装済み・コミット fdb3805）として処理。仮番号 #33 / #34 / #35 / #36 を現行ストック採番ルール（通し番号方式）に従い #46 / #47 / #48 / #49 として新規追加（4 件）。新規 #46（ContactUpdateForm 動的確認 CB の「confirmed_at is None 限定」明文化、コミット 151f432）／#47（Contact.lang blank=True 明文化、コミット 32ef99d）／#48（§10.4.3 行 1117 削除、fix での set_primary_contact 記述・§11.4.1 との矛盾解消、たんたん判断で案 A 確定、コミット 1caec0a）／#49（15 番 DuplicateCandidateGroupListView 絞り込み + サイドバー明文化、コミット 4f605c8）。#46〜#49 は新規セクション「A 側追加実装項目（#46〜#49）」として #44 の後ろに配置。整合性チェックリスト統合最終版セクションに #31 のチェック項目更新（実装済み内容を反映）、#46 / #47 / #48 / #49 関連項目（#48 は old_primary_new_status 表整理を含めて 2 行）、および連動確認（#46 と #25、#48 と #22）を追加。本依頼は当初「仕様書本体 .md の直接改訂依頼」として投下されたが、A 側ブランチ未マージのため運用ルール（ストック追加 → 仕様書本体改訂は実装完了マージ後）に従いストック追加として処理することをたんたん確認のうえ確定。**#45 欠番について**：本セッション内の議論経緯として、第12版作成直前にたんたんから「§10.4.3 行 1117 削除」の登録依頼があり、私が「#45 として登録予定」と回答した。その直後にサポート担当クロード君の投下プロンプトが届き、依頼 5 件のうち §10.4.3 行 1117 削除は仮番号 #35 として一括依頼に含まれていることが判明。「#31 仮番号は現行 #31 のステータス更新」「残り 4 件は #46〜#49 として新規追加」と整理し直した結果、当初 #45 として採番予定だった §10.4.3 行 1117 削除は #48 として収録される運びとなり、#45 は欠番のまま処理することとした。次回追加項目は #50 から採番する。 |
